---
phase: 01-auditable-log-pipeline-analysis
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - pipeline/__main__.py
  - pipeline/models.py
  - pipeline/ingest.py
  - pipeline/validation.py
  - tests/pipeline/test_validation.py
autonomous: true
requirements:
  - PIPE-01
  - PIPE-02
  - PIPE-03
  - PIPE-04
estimate:
  tokens: 38000
  raw_tokens: 38000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Every physical source line receives one stably ordered validation result before or after parsing; malformed JSON is not lost and later exact duplicates cross-reference the first retained source line per D-02/D-03."
    - "A parseable record with no detected issue receives ACCEPT; every independently detected issue remains visible; final action precedence is REJECT over REPAIR over ACCEPT per D-01/D-05."
    - "Required fields, types, timestamp awareness, known levels, content rules, unexpected fields, and exact duplicates have stable issue codes and deterministic policy rationales."
    - "The fixed known-level allowlist is enforced, while non-empty unknown services remain valid because no authoritative service allowlist is supplied."
  flagged_assumptions:
    - requirement_id: PIPE-01
      category: empty
      status: unresolved
      assumption: "The source artifacts do not specify the contract for a wholly empty input file; the supplied canonical file is non-empty and remains the acceptance target."
    - requirement_id: PIPE-02
      category: adjacency
      status: unresolved
      assumption: "The source artifacts require every independent issue to be retained but do not specify whether repeated instances of the same issue code on one record are collapsed; the issue catalogue must document the chosen stable behavior."
    - requirement_id: PIPE-02
      category: empty
      status: unresolved
      assumption: "Null and missing required values are rejected, but the source artifacts do not define a separate empty-dataset validation result."
    - requirement_id: PIPE-03
      category: concurrency
      status: unresolved
      assumption: "Validation functions are deterministic, but coordinated concurrent writes to one output root are not an approved Phase 1 contract."
    - requirement_id: PIPE-04
      category: unclassified
      status: unresolved
      assumption: "The deterministic probe could not classify the ledger requirement; the plan treats one ledger record per physical line with nested issue/normalization entries as the auditable shape."
  prohibitions: []
  artifacts:
    - path: "pipeline/models.py"
      provides: "Typed source, issue, normalization, ledger, disposition, and clean-record contracts"
      exports: ["Disposition", "Issue", "Normalization", "SourceEnvelope", "LedgerEntry", "CleanRecord"]
    - path: "pipeline/ingest.py"
      provides: "Bounded physical-line reader, JSON parser, and canonical full-record digest"
      exports: ["iter_source_lines", "parse_json_line", "canonical_record_digest"]
    - path: "pipeline/validation.py"
      provides: "Stable issue catalogue, full issue collection, duplicate policy, and final-action precedence"
      exports: ["ISSUE_POLICIES", "validate_record", "choose_final_action"]
    - path: "pipeline/__main__.py"
      provides: "Independently runnable full-input validate stage"
      contains: "cmd_validate"
    - path: "tests/pipeline/test_validation.py"
      provides: "Unit and real-source tests for all validation/disposition contracts"
  key_links:
    - from: "pipeline/ingest.py"
      to: "pipeline/validation.py"
      via: "SourceEnvelope is constructed before parse and passed with raw provenance"
      pattern: "SourceEnvelope|validate_record"
    - from: "pipeline/validation.py"
      to: "pipeline/models.py"
      via: "all issue policies resolve through the single Disposition precedence contract"
      pattern: "Disposition|choose_final_action"
    - from: "pipeline/__main__.py"
      to: "pipeline/ingest.py"
      via: "validate streams every bounded physical line and emits source-ordered ledger records"
      pattern: "cmd_validate|iter_source_lines"
---

## Phase Goal

**As a** reviewer, **I want to** run and trace the local log pipeline end-to-end, **so that** every claimed result is reproducible, source-grounded, and defensible.

<objective>
Expand the proven tracer into complete line-accounting and explicit validation/disposition behavior over the immutable seven-day input.

Purpose: Give the reviewer a defensible, deterministic decision for every source line before any full dataset is materialized.
Output: Typed validation contracts, line-envelope ingestion, the full issue/disposition policy, an independently runnable validate command, and focused tests implementing D-01 through D-05.
</objective>

<execution_context>
@/home/dangnhatminh/.codex/gsd-core/workflows/execute-plan.md
@/home/dangnhatminh/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-auditable-log-pipeline-analysis/01-CONTEXT.md
@.planning/phases/01-auditable-log-pipeline-analysis/01-RESEARCH.md
@.planning/phases/01-auditable-log-pipeline-analysis/01-PATTERNS.md
@.planning/phases/01-auditable-log-pipeline-analysis/01-01-SUMMARY.md
@pipeline/__main__.py
@tests/pipeline/test_tracer.py
@docs/onboard/datapack/README.md
</context>

<interfaces>
Preserve `python -m pipeline trace --input PATH --source-line N --output-root PATH --max-line-bytes N`. The phase-wide ledger fields are `source_path`, `source_sha256`, `source_line`, `record_digest`, `raw_line`, `issues`, `normalizations`, `final_action`, and `retained_source_line`; issue fields are `issue_code`, `field`, `original_value`, `normalized_value`, `action`, and `reason`.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make malformed and schema-invalid lines visible through the validate command</name>
  <read_first>
    - `.planning/phases/01-auditable-log-pipeline-analysis/01-CONTEXT.md` — read D-01, D-02, D-04, and D-05 plus the observed anomaly list.
    - `.planning/phases/01-auditable-log-pipeline-analysis/01-RESEARCH.md` — read `Envelope Every Input Line Before Parsing`, the first resolved open question, and pitfalls 1–2.
    - `.planning/phases/01-auditable-log-pipeline-analysis/01-PATTERNS.md` — use the assigned `models.py`, `ingest.py`, and `validation.py` boundaries.
    - `pipeline/__main__.py` — preserve the tracer command and reuse its path/error guards.
    - `docs/onboard/datapack/data/app_logs_7days.jsonl` — treat as read-only and use malformed, missing-level, invalid-time, and optional-trace cases as real fixtures.
  </read_first>
  <reversibility rating="costly">The ledger shape and stable issue codes are consumed by storage, SQL, report rendering, and later submission audit.</reversibility>
  <files>pipeline/models.py, pipeline/ingest.py, pipeline/validation.py, pipeline/__main__.py, tests/pipeline/test_validation.py</files>
  <behavior>
    - Test 1: malformed JSON becomes one ledger record with `JSON_MALFORMED`, final action REJECT, source line, raw line, and no invented parsed fields per D-02.
    - Test 2: one-based `SourceEnvelope` provenance is assigned before decoding/parsing, and a line above 1 MiB is rejected before JSON parsing.
    - Test 3: required `timestamp`, `service`, `level`, `message`, and `request_id` fields receive stable missing/type/empty issues; invalid or offset-less timestamps and unknown levels receive dedicated issues.
    - Test 4: non-empty unknown services remain valid because no supplied allowlist is authoritative; `trace_id` is optional and unexpected fields are explicitly classified.
    - Test 5: `python -m pipeline validate` streams the complete immutable input and writes one source-line-ordered ledger record per physical line.
  </behavior>
  <action>Create immutable `Disposition`, `Issue`, `Normalization`, `SourceEnvelope`, `LedgerEntry`, and `CleanRecord` contracts with the declared fields. Implement `iter_source_lines` to attach source hash/path, assign one-based provenance, and enforce the default `1048576`-byte ceiling before decoding or parsing; implement `parse_json_line` and SHA-256 `canonical_record_digest` over the complete canonical parsed object. Define stable English issue policies for JSON, required-field, type, empty-content, timestamp-awareness, level-category, and unexpected-field conditions. Per D-01/D-02, do not infer missing values. Enforce the resolved research choice: `INFO|WARN|ERROR` is the strict level allowlist, but any non-empty service remains valid absent a supplied service allowlist. Wire `python -m pipeline validate` with the established `--input`, `--output-root`, and `--max-line-bytes` contracts, keeping `trace` intact. Start with failing tests.</action>
  <verify>
    <automated>uv run pytest tests/pipeline/test_validation.py tests/pipeline/test_tracer.py -q -k 'not duplicate' &amp;&amp; uv run ruff check pipeline/models.py pipeline/ingest.py pipeline/validation.py pipeline/__main__.py tests/pipeline/test_validation.py</automated>
  </verify>
  <acceptance_criteria>
    - Every physical input line is enveloped before parsing and malformed input remains traceable.
    - Stable issue codes cover JSON, schema, type, timestamp, categorical, and content-quality cases.
    - The level/service policy exactly matches the resolved research recommendation.
    - The validate command is independently runnable and preserves tracer behavior.
  </acceptance_criteria>
  <done>A reviewer can run validation across the immutable source and inspect deterministic decisions for malformed and schema-invalid lines.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Complete duplicate handling, multi-issue precedence, and conservative repair</name>
  <read_first>
    - `.planning/phases/01-auditable-log-pipeline-analysis/01-CONTEXT.md` — read D-01 through D-05 verbatim.
    - `.planning/phases/01-auditable-log-pipeline-analysis/01-RESEARCH.md` — read `Collect All Issues`, the second resolved open question, and expected reconciliation counts.
    - `pipeline/models.py`, `pipeline/ingest.py`, `pipeline/validation.py`, `pipeline/__main__.py` — extend the exact contracts created in Task 1.
    - `tests/pipeline/test_validation.py` — retain Task 1 cases and add multi-issue/duplicate/repair-branch coverage.
  </read_first>
  <files>pipeline/validation.py, pipeline/__main__.py, tests/pipeline/test_validation.py</files>
  <behavior>
    - Test 1: every independently applicable issue is retained in deterministic catalogue order and one action follows REJECT over REPAIR over ACCEPT per D-05.
    - Test 2: the first full-record canonical digest is retained and each later exact duplicate receives `EXACT_DUPLICATE`, REJECT, and the first `retained_source_line` per D-03.
    - Test 3: a parseable issue-free record receives ACCEPT, while malformed JSON, invalid timestamps, and required-field failures receive REJECT per D-02.
    - Test 4: an explicitly repairable synthetic policy can reach REPAIR, but no canonical-source defect is forced into that branch per D-01.
    - Test 5: a record with both repairable and rejecting issues retains both and resolves to REJECT; repeated validation yields identical issue and record ordering.
  </behavior>
  <action>Complete `ISSUE_POLICIES`, `validate_record`, and `choose_final_action` so each applicable issue remains visible before D-05 precedence resolves one action. Track first-seen canonical full-record digests across the streaming run; per D-03 reject later exact duplicates and record the retained source line. Implement the REPAIR branch as a first-class disposition used only by policies whose value is lossless, unambiguous, and mechanically provable per D-01; the canonical run must honestly remain at zero repairs when no source defect meets that bar. Ensure `cmd_validate` emits deterministic nested issue/normalization order and stable source-line order without editing the source or inventing analytical values. Write the new tests before changing implementation.</action>
  <verify>
    <automated>uv run pytest tests/pipeline/test_validation.py tests/pipeline/test_tracer.py -q &amp;&amp; uv run ruff check pipeline/models.py pipeline/ingest.py pipeline/validation.py pipeline/__main__.py tests/pipeline/test_validation.py &amp;&amp; uv run ruff format --check pipeline/models.py pipeline/ingest.py pipeline/validation.py pipeline/__main__.py tests/pipeline/test_validation.py</automated>
  </verify>
  <acceptance_criteria>
    - Every issue is retained and one final action follows D-05 precedence.
    - Later exact duplicates point to the first retained source line per D-03.
    - The REPAIR branch exists but no canonical defect is relabeled to make its count non-zero.
    - Focused tests cover issue collection, precedence, exact duplicates, optional fields, unexpected fields, and the real anomaly classes.
  </acceptance_criteria>
  <done>The reviewer can trace every source line to a stable issue set, rationale, and one conservative final disposition.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Untrusted JSONL bytes → parser/validator | Malformed, oversized, structurally invalid, duplicated, or misleading records enter deterministic code. |
| Validation result → downstream analytical path | Rejected records must remain attributable and must not be mistaken for clean rows. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-SC | Tampering | Locked Python dependencies | high | mitigate | Consume only the human-approved Plan 01 `uv.lock`; add no new package-manager install surface. |
| T-01-06 | Denial of Service | `iter_source_lines` / JSON parsing | medium | mitigate | Stream one line at a time and reject lines above the documented 1 MiB maximum before parsing. |
| T-01-07 | Repudiation | validation/disposition | medium | mitigate | One result per physical line retains raw provenance, all issue reasons/actions, final action, source hash, and duplicate cross-reference. |
| T-01-08 | Information Disclosure | diagnostics and ledger | low | accept | The supplied records are fictional and the ledger is required evidence; ordinary diagnostics avoid unrelated bulk dumps. |
| T-01-09 | Spoofing | provenance fields | medium | mitigate | Bind source-line identifiers to byte-level source SHA-256 and canonical record digest rather than record-supplied identifiers. |
</threat_model>

<verification>
Run focused unit and real-source validation tests, invoke the validate command against a temporary output root, confirm one ordered result per physical line, and compare the supplied source hash before and after.
</verification>

<success_criteria>
- Every source line is represented once and every independently detected issue remains visible.
- D-01 through D-05 govern repair, rejection, duplicate handling, normalization evidence, and action precedence exactly.
- The two resolved research choices are explicit in tests and executable behavior.
- The typed validation contracts are ready for the storage expansion without interface changes.
</success_criteria>

<output>
Create `.planning/phases/01-auditable-log-pipeline-analysis/01-02-SUMMARY.md` when done.
</output>
