---
phase: 01-auditable-log-pipeline-analysis
plan: 07
type: execute
wave: 7
depends_on:
  - 01-06
files_modified:
  - pipeline/manifest.py
  - tests/pipeline/test_evidence.py
  - .planning/ROADMAP.md
autonomous: true
gap_closure: true
requirements:
  - RPRO-02
estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "During standalone verification, `verify_run_manifest()` freshly inventories every regular file under `docs/onboard` and refuses any difference from both `source_manifest.json` and `run_manifest.json`, closing RPRO-02 and roadmap Phase 1 success criterion 1."
    - "A forged or stale saved source inventory remains rejected after `build_run_manifest()` rebuilds all derived run-manifest hashes and the content-derived run ID, completing D-17 integrity assurance."
    - "The Phase 1 roadmap goal is a canonical `As a ..., I want to ..., so that ... .` user story while the phase title, MVP mode, dependency, requirements, four technical success criteria, and executed-plan history remain unchanged."
    - "The targeted adversarial test and `make verify-phase1` pass without changing any supplied source file."
  artifacts:
    - path: "pipeline/manifest.py"
      provides: "Fresh live supplied-input inventory verification against both persisted evidence layers"
      contains: "inventory_supplied_inputs"
    - path: "tests/pipeline/test_evidence.py"
      provides: "Regression proving a forged persisted inventory cannot be accepted after manifest rebuild"
      contains: "verify_run_manifest"
    - path: ".planning/ROADMAP.md"
      provides: "Canonical Phase 1 MVP user-story goal with unchanged technical scope and success criteria"
      contains: "As a reviewer, I want to run the complete log pipeline and customer analysis, so that I can defend every result from immutable source evidence."
  key_links:
    - from: "pipeline/manifest.py"
      to: "pipeline/integrity.py"
      via: "`verify_run_manifest()` calls `inventory_supplied_inputs()` at verification time rather than deriving the verdict only from saved JSON"
      pattern: "inventory_supplied_inputs"
    - from: "pipeline/manifest.py"
      to: "data/evidence/phase1/source_manifest.json, data/evidence/phase1/run_manifest.json"
      via: "one fresh inventory is compared for exact ordered equality with each persisted `source_inventory`"
      pattern: "source_inventory"
    - from: "tests/pipeline/test_evidence.py"
      to: "pipeline/manifest.py"
      via: "forge source-manifest inventory, rebuild run manifest, then require verification failure"
      pattern: "build_run_manifest|verify_run_manifest"
    - from: ".planning/ROADMAP.md"
      to: "GSD canonical user-story validator"
      via: "the single Phase 1 Goal line passes `user-story validate` without changing the success criteria"
      pattern: "As a reviewer, I want to .*, so that .*\\."
---

## Phase Goal

**As a** reviewer, **I want to** run the complete log pipeline and customer analysis, **so that** I can defend every result from immutable source evidence.

<objective>
Close the two Phase 1 verification blockers without reopening completed product scope: bind standalone evidence verification to the live supplied bytes, and make the roadmap's existing MVP goal canonical.

Purpose: Ensure a regenerated manifest cannot turn forged or stale source-integrity claims into accepted evidence, while restoring authoritative MVP validation for the already-delivered technical workflow.
Output: A fresh-inventory verification guard and adversarial regression for RPRO-02/D-17, plus a one-line Phase 1 roadmap-goal correction that preserves all existing success criteria.
</objective>

<execution_context>
@/home/dangnhatminh/.codex/gsd-core/workflows/execute-plan.md
@/home/dangnhatminh/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/01-auditable-log-pipeline-analysis/01-CONTEXT.md
@.planning/phases/01-auditable-log-pipeline-analysis/01-RESEARCH.md
@.planning/phases/01-auditable-log-pipeline-analysis/01-VERIFICATION.md
@.planning/phases/01-auditable-log-pipeline-analysis/01-06-SUMMARY.md
@pipeline/integrity.py
@pipeline/manifest.py
@tests/pipeline/test_evidence.py
@Makefile
</context>

<interfaces>
`pipeline.integrity.inventory_supplied_inputs(supplied_root: Path = SUPPLIED_ROOT) -> list[dict[str, str]]` returns every regular file below `docs/onboard` in stable relative-path order with `path` and `sha256`. `pipeline.manifest.build_run_manifest(output_root: Path) -> dict[str, Any]` intentionally builds derived evidence from existing generated artifacts. `pipeline.manifest.verify_run_manifest(output_root: Path) -> None` is the fail-closed boundary and raises `ManifestVerificationError` for stale or forged evidence. Preserve those public signatures and the existing run-manifest schema.
</interfaces>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Reject forged source inventories with a fresh live-byte comparison</name>
  <read_first>
    - `.planning/phases/01-auditable-log-pipeline-analysis/01-VERIFICATION.md` — read the two failed truths, adversarial spot-check, RPRO-02 result, and D-17 gap; do not convert the two clean-uv behavior-unverified observations into code work.
    - `.planning/phases/01-auditable-log-pipeline-analysis/01-CONTEXT.md` — read D-17 and the immutable evidence boundary.
    - `pipeline/integrity.py` — reuse `inventory_supplied_inputs()` and its stable `path`/`sha256` list contract; do not create another filesystem scanner or hash implementation.
    - `pipeline/manifest.py` — preserve `_manifest_payload()`, `build_run_manifest()`, and `verify_run_manifest()` public behavior except for the new fail-closed live-inventory check.
    - `tests/pipeline/test_evidence.py` — extend the existing manifest tamper-test style and `_build_evidence()` fixture path.
    - `Makefile` — use the existing host-aware locked-uv/existing-venv verification path rather than adding an environment workaround.
  </read_first>
  <files>pipeline/manifest.py, tests/pipeline/test_evidence.py</files>
  <behavior>
    - Test 1: after ordinary evidence generation and manifest build, `verify_run_manifest()` accepts when the freshly inventoried `docs/onboard` list exactly equals both persisted inventory lists.
    - Test 2 (`test_manifest_verification_rejects_forged_source_inventory_after_rebuild`): alter a `source_manifest.json` inventory entry in a temporary output root, call `build_run_manifest()` so `source_manifest_sha256`, the run-manifest copy, artifact descriptors, and `run_id` are consistently rebuilt around the forgery, then require `verify_run_manifest()` to raise `ManifestVerificationError` for a source-inventory mismatch.
    - Test 3 (`test_manifest_verification_requires_three_way_source_inventory_equality`): a mismatch between `source_manifest.json` and `run_manifest.json` fails even when the live inventory equals one of them; verification requires three-way equality among live bytes and both persisted expectations.
    - Test 4: the regression never mutates `docs/onboard`; only JSON inside the test's temporary generated output root is forged.
  </behavior>
  <action>Start with the adversarial failing regression that reproduces the verifier's accepted-forgery sequence exactly: generate evidence under `tmp_path`, build the run manifest, replace one persisted source-inventory digest or membership entry in `source_manifest.json`, rebuild the run manifest from that forged source manifest, and prove the current verifier accepts before implementation. Then import and call `inventory_supplied_inputs()` from `pipeline.integrity` inside the verification path. Read `source_manifest.json` once for the verification checks, validate that each persisted `source_inventory` is a list, and compare the freshly computed complete ordered inventory for exact equality against both the source-manifest expectation and the run-manifest expectation before accepting artifact hashes or the rebuilt `run_id`. Raise `ManifestVerificationError` with a stable message that identifies which persisted inventory disagrees, while avoiding disclosure of full source contents. Preserve the manifest builder's deterministic schema and public signatures: the security property belongs at the independent verification boundary, and rebuilding derived metadata must not make stale source claims self-authenticating. This implements RPRO-02, roadmap success criterion 1, and D-17 without changing D-14 stage commands or adding dependencies.</action>
  <verify>
    <automated>if command -v uv &gt;/dev/null 2&gt;&amp;1; then uv run --locked pytest tests/pipeline/test_evidence.py -q -k 'source_inventory'; else .venv/bin/python -m pytest tests/pipeline/test_evidence.py -q -k 'source_inventory'; fi &amp;&amp; make verify-phase1</automated>
  </verify>
  <acceptance_criteria>
    - A fresh complete `docs/onboard` inventory is computed during every standalone run-manifest verification.
    - Verification requires exact ordered equality among the live inventory, `source_manifest.json.source_inventory`, and `run_manifest.json.source_inventory`.
    - The adversarial test rejects a forged or stale inventory even after rebuilding the run manifest and all derived hashes around it.
    - Existing artifact, SQL, count, link, and deterministic-run verification behavior remains covered and passing.
    - The regression writes only below `tmp_path`; `git diff --exit-code -- docs/onboard` remains clean.
  </acceptance_criteria>
  <done>Standalone manifest verification derives source integrity from current supplied bytes and cannot accept a self-consistent forged persisted inventory.</done>
</task>

<task type="auto">
  <name>Task 2: Restore canonical MVP goal validation without changing Phase 1 scope</name>
  <read_first>
    - `.planning/phases/01-auditable-log-pipeline-analysis/01-VERIFICATION.md` — read the MVP Mode Contract blocker and its requirement to correct the roadmap goal.
    - `.planning/ROADMAP.md` — limit the edit to the Phase 1 `Goal` line; preserve Mode, dependency, requirement IDs, all four success criteria, execution status, wave list, and later phases.
    - `/home/dangnhatminh/.codex/gsd-core/references/user-story-template.md` — apply the canonical actor/capability/outcome grammar.
  </read_first>
  <files>.planning/ROADMAP.md</files>
  <action>Replace only the Phase 1 Goal value with `As a reviewer, I want to run the complete log pipeline and customer analysis, so that I can defend every result from immutable source evidence.` Keep it on one line and leave `**Mode:** mvp` immediately below it. This wording preserves the existing actor, complete local pipeline/customer-analysis capability, immutable-source evidence basis, and reviewer-defensibility outcome while satisfying the canonical validator. Do not change the phase title, dependency, RPRO/PIPE requirement mapping, any of the four technical success criteria, executed plan count/list, progress table, or later-phase content. Treat this as correction of planning metadata, not new product work.</action>
  <verify>
    <automated>STORY=$(sed -n '/^### Phase 1:/,/^### Phase 2:/s/^\*\*Goal\*\*: //p' .planning/ROADMAP.md) &amp;&amp; node "$HOME/.codex/gsd-core/bin/gsd-tools.cjs" user-story validate --story "$STORY" &amp;&amp; test "$(sed -n '/^### Phase 1:/,/^### Phase 2:/p' .planning/ROADMAP.md | grep -c '^  [1-4]\. A reviewer can')" -eq 4 &amp;&amp; git diff --check -- .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - The Phase 1 Goal passes the GSD `user-story validate` command with non-empty reviewer, capability, and outcome slots.
    - `**Mode:** mvp` remains present once immediately below the Goal line.
    - The Phase 1 title, dependency, 13 requirement IDs, four success criteria, 6/6 executed-plan history, and all later phase sections are unchanged.
    - No application or generated-evidence file is modified by this task.
  </acceptance_criteria>
  <done>Formal MVP verification can consume a canonical Phase 1 user story whose technical scope is identical to the already-executed roadmap contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Live `docs/onboard/**` bytes → standalone manifest verifier | Current filesystem membership and bytes must be measured independently of persisted claims. |
| Generated source/run manifests → verification verdict | Persisted JSON is attacker- or corruption-controlled evidence and cannot authenticate itself. |
| Roadmap planning metadata → MVP validator/executor | A wording fix must not silently alter technical success criteria or product scope. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-SC | Tampering | Existing uv/DuckDB/pytest/Ruff execution | high | mitigate | Add no packages or installs; retain the human-approved committed lock and run the existing `make verify-phase1` lock/test/lint gate. |
| T-01-20 | Tampering | `verify_run_manifest()` source-integrity verdict | high | mitigate | Recompute every live supplied-file digest and require exact three-way equality with both persisted inventory lists before accepting linked evidence. |
| T-01-21 | Repudiation | Forged/stale manifest rebuild sequence | medium | mitigate | Commit a deterministic adversarial regression that rebuilds derived metadata around a forgery and still requires a named verification failure. |
| T-01-22 | Tampering | Phase 1 roadmap contract | medium | mitigate | Change one Goal line only, validate canonical grammar, and assert all four technical success criteria and existing plan history remain present. |
| T-01-23 | Information Disclosure | Inventory mismatch diagnostics | low | accept | Relative supplied paths and digests are already required evidence; errors identify the mismatched evidence layer without dumping source contents. |
</threat_model>

## Artifacts this phase produces

- Updated symbol behavior: `verify_run_manifest(output_root)` now calls the existing `inventory_supplied_inputs()` function and requires the live, source-manifest, and run-manifest inventories to agree before the evidence graph is accepted.
- Regression evidence: `tests/pipeline/test_evidence.py` contains the adversarial forged/stale-inventory-after-rebuild case and retains all prior manifest/report tamper checks.
- Planning contract: `.planning/ROADMAP.md` contains one canonical Phase 1 MVP Goal line; its phase title, mode, dependencies, requirements, four success criteria, and completed Plans 01–06 remain intact.
- No new dependency, CLI flag, manifest field, generated output path, or application subsystem is introduced.

## Multi-Source Coverage Audit

| Source | ID | Feature / requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Reviewer-runnable and defensible immutable-source log-quality/customer-analysis workflow | 01-01–01-07 | COVERED | Task 2 corrects only canonical story shape; Tasks/Plans 01–06 retain technical delivery. |
| REQ | RPRO-01 | Locked clean-checkout environment and commands | 01-01, 01-06 | COVERED | Present behavior remains behavior-unverified on this no-uv host; no application gap or fabricated resolution is added. |
| REQ | RPRO-02 | Verify supplied inputs remain unchanged through recorded hashes | 01-01, 01-03, 01-06, 01-07 | COVERED | Task 1 closes the standalone live-inventory gap. |
| REQ | PIPE-01 | Provenance-preserving physical-line ingestion | 01-01, 01-02 | COVERED | Existing completed scope unchanged. |
| REQ | PIPE-02 | Stable issue detection | 01-02 | COVERED | Existing completed scope unchanged. |
| REQ | PIPE-03 | Explicit accept/repair/reject rules | 01-02 | COVERED | Existing completed scope unchanged. |
| REQ | PIPE-04 | Per-record quality ledger | 01-02, 01-03 | COVERED | Existing completed scope unchanged. |
| REQ | PIPE-05 | Row conservation and deterministic reruns | 01-01, 01-03, 01-06 | COVERED | Existing completed scope unchanged. |
| REQ | PIPE-06 | Typed Parquet and schema rationale | 01-01, 01-03, 01-06 | COVERED | Existing completed scope unchanged. |
| REQ | PIPE-07 | Highest-ERROR service analysis | 01-04, 01-06 | COVERED | Existing completed scope unchanged. |
| REQ | PIPE-08 | Daily pattern and qualified unusual-day rule | 01-04, 01-06 | COVERED | Existing completed scope unchanged. |
| REQ | PIPE-09 | Top normalized error analysis | 01-05, 01-06 | COVERED | Existing completed scope unchanged. |
| REQ | PIPE-10 | Rejected/repaired quality reconciliation | 01-05, 01-06 | COVERED | Existing completed scope unchanged. |
| REQ | PIPE-11 | Direct dataset/SQL/result/manifest evidence chain | 01-01, 01-04–01-06 | COVERED | Existing completed scope unchanged. |
| RESEARCH | — | Full sorted supplied-file SHA-256 inventory before/after and in reviewer evidence | 01-03, 01-06, 01-07 | COVERED | Task 1 adds the missing independent live check at verify time. |
| RESEARCH | — | Deterministic manifests and content-derived run IDs | 01-06, 01-07 | COVERED | Builder contract remains stable; verifier no longer trusts it as its own source of truth. |
| RESEARCH | — | Provenance ledger, typed Parquet, checked-in SQL, and evidence-only report | 01-02–01-06 | COVERED | Outside gap scope and preserved unchanged. |
| RESEARCH | — | Immutable generated-output boundary and explicit source-tree guard | 01-01, 01-03, 01-06, 01-07 | COVERED | Adversarial test forges only temporary generated JSON. |
| CONTEXT | D-01–D-05 | Conservative disposition, duplicates, normalization evidence, and precedence | 01-02, 01-03, 01-05 | COVERED | Existing completed scope unchanged. |
| CONTEXT | D-06–D-09 | ERROR normalization and unclassified visibility | 01-01, 01-03, 01-05, 01-06 | COVERED | Existing completed scope unchanged. |
| CONTEXT | D-10–D-13 | UTC days and qualified unusual-day analysis | 01-03, 01-04, 01-06 | COVERED | Existing completed scope unchanged. |
| CONTEXT | D-14–D-16 | Canonical/stage commands, primary report, and direct evidence chain | 01-01, 01-06 | COVERED | Existing commands and report interfaces remain unchanged. |
| CONTEXT | D-17 | Regenerated deterministic snapshot checks hashes/internal consistency | 01-03, 01-06, 01-07 | COVERED | Task 1 prevents rebuilt persisted metadata from authenticating a false source inventory. |

## Verification scope notes

- The verifier's two clean-uv items remain `PRESENT_BEHAVIOR_UNVERIFIED` on this host because no `uv` executable is available and no clean environment was observed. They are not application gaps, and this plan does not invent a substitute observation.
- The targeted test uses locked `uv` when available and the repository's existing `.venv` fallback on this host, matching the current Makefile. `make verify-phase1` remains the direct complete gate.

<verification>
Run the targeted forged/stale source-inventory regression through the available locked runtime path, then run `make verify-phase1`. Confirm the full suite, Ruff gates, artifact regeneration/verification, and `git diff --exit-code -- docs/onboard` all pass. Separately extract the Phase 1 Goal line and run the GSD canonical user-story validator.
</verification>

<success_criteria>
- RPRO-02 and roadmap success criterion 1 are satisfied by a live complete inventory check during standalone verification.
- D-17 cannot be satisfied by rebuilding a self-consistent run manifest around a forged or stale source inventory.
- The Phase 1 MVP Goal is canonical without technical-scope, requirement, success-criteria, or plan-history drift.
- The clean-uv observations remain honestly behavior-unverified until an actual clean uv-backed run is available.
</success_criteria>

<output>
Create `.planning/phases/01-auditable-log-pipeline-analysis/01-07-SUMMARY.md` when done.
</output>
