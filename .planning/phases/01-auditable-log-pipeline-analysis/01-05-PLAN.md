---
phase: 01-auditable-log-pipeline-analysis
plan: 05
type: execute
wave: 5
depends_on:
  - 01-04
files_modified:
  - pipeline/sql/03_top_normalized_errors.sql
  - pipeline/sql/04_quality_reconciliation.sql
  - tests/pipeline/test_analysis.py
  - data/evidence/phase1/tables/03_top_normalized_errors.csv
  - data/evidence/phase1/tables/04_quality_reconciliation.csv
autonomous: true
requirements:
  - PIPE-09
  - PIPE-10
  - PIPE-11
estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Top-three SQL ranks primary semantic ERROR types with count-descending then name-ascending order, retains service contributions, keeps equal-count types as separate rows, and exposes `UNCLASSIFIED_ERROR` per D-06 through D-09."
    - "Quality SQL reports issue occurrences separately from record totals by final action, includes explicit ACCEPT/REPAIR/REJECT rows including zero REPAIR, and proves both input/ledger and Parquet/reject conservation equations."
    - "All four registry entries are now runnable as one no-ID analysis command and regenerate four deterministic tables from checked-in SQL without Python aggregate calculations."
  flagged_assumptions:
    - requirement_id: PIPE-09
      category: unclassified
      status: unresolved
      assumption: "The deterministic probe could not classify top-three grouping; the plan ranks primary semantic error_type and reports service contributions as secondary evidence per D-06/D-07."
    - requirement_id: PIPE-10
      category: unclassified
      status: unresolved
      assumption: "The deterministic probe could not classify quality grouping; the result exposes both issue-occurrence rows and record-level final-action rows so the two counting units cannot be confused."
    - requirement_id: PIPE-11
      category: empty
      status: unresolved
      assumption: "The supplied canonical dataset contains analytical rows; the source artifacts do not define customer-answer wording for a completely empty cleaned dataset."
  prohibitions: []
  artifacts:
    - path: "pipeline/sql/03_top_normalized_errors.sql"
      provides: "Deterministic top-three semantic error types with service contributions"
    - path: "pipeline/sql/04_quality_reconciliation.sql"
      provides: "Issue-occurrence and final-action record counts with conservation totals"
    - path: "data/evidence/phase1/tables/03_top_normalized_errors.csv"
      provides: "Recorded result for customer question 3"
    - path: "data/evidence/phase1/tables/04_quality_reconciliation.csv"
      provides: "Recorded result for customer question 4"
    - path: "tests/pipeline/test_analysis.py"
      provides: "Complete four-query schemas, tie behavior, counting-unit separation, and deterministic rerun tests"
  key_links:
    - from: "pipeline/sql/03_top_normalized_errors.sql"
      to: "data/processed/logs_clean.parquet"
      via: "parameterized Parquet query groups primary error_type and aggregates service contributions"
      pattern: "read_parquet|error_type"
    - from: "pipeline/sql/04_quality_reconciliation.sql"
      to: "data/evidence/phase1/quality_ledger.jsonl"
      via: "parameterized JSONL read and issue-array unnesting"
      pattern: "read_json|issues"
    - from: "pipeline/analysis.py"
      to: "pipeline/sql/03_top_normalized_errors.sql, pipeline/sql/04_quality_reconciliation.sql"
      via: "Plan 04 registry IDs resolve the new static SQL without interface changes"
      pattern: "top-normalized-errors|quality-reconciliation"
---

## Phase Goal

**As a** reviewer, **I want to** run and trace the local log pipeline end-to-end, **so that** every claimed result is reproducible, source-grounded, and defensible.

<objective>
Complete the static-SQL analysis set with top semantic errors and auditable quality reconciliation.

Purpose: Answer the remaining two customer questions while preserving the difference between primary error taxonomy, secondary dimensions, issue occurrences, and record-level dispositions.
Output: Two checked-in SQL files, two committed deterministic tables, and complete analysis tests implementing D-01 through D-09 and D-12.
</objective>

<execution_context>
@/home/dangnhatminh/.codex/gsd-core/workflows/execute-plan.md
@/home/dangnhatminh/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-auditable-log-pipeline-analysis/01-CONTEXT.md
@.planning/phases/01-auditable-log-pipeline-analysis/01-RESEARCH.md
@.planning/phases/01-auditable-log-pipeline-analysis/01-PATTERNS.md
@.planning/phases/01-auditable-log-pipeline-analysis/01-04-SUMMARY.md
@pipeline/analysis.py
@pipeline/sql/01_service_error_counts.sql
@pipeline/sql/02_daily_error_counts.sql
@data/evidence/phase1/quality_ledger.jsonl
@data/evidence/phase1/schema.json
@docs/onboard/01_Domain_POC.md
</context>

<interfaces>
Use Plan 04 registry IDs `top-normalized-errors` and `quality-reconciliation` with their already-declared SQL paths, result paths, ordered parameter names, and expected columns. Table 03 fields are `rank`, `error_type`, `error_count`, `service_contributions_json`, `unclassified_error_count`; table 04 fields are `metric_type`, `issue_code`, `final_action`, `record_count`, `issue_occurrences`, `is_reconciled`.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reproduce the top three semantic ERROR types and associated services</name>
  <read_first>
    - `docs/onboard/01_Domain_POC.md` — read the top-three error question.
    - `.planning/phases/01-auditable-log-pipeline-analysis/01-CONTEXT.md` — read D-06 through D-09 and D-12.
    - `.planning/phases/01-auditable-log-pipeline-analysis/01-RESEARCH.md` — read pitfall 4 and the customer-analysis responsibility boundary.
    - `pipeline/analysis.py`, `pipeline/sql/01_service_error_counts.sql`, `pipeline/sql/02_daily_error_counts.sql`, `tests/pipeline/test_analysis.py` — extend the established static-query/result contract.
    - `data/evidence/phase1/schema.json` — group the primary `error_type` and retain services only as secondary evidence.
  </read_first>
  <files>pipeline/sql/03_top_normalized_errors.sql, tests/pipeline/test_analysis.py, data/evidence/phase1/tables/03_top_normalized_errors.csv</files>
  <behavior>
    - Test 1: SQL filters cleaned ERROR rows, groups primary `error_type`, orders `error_count DESC, error_type ASC`, and returns exactly three rows when at least three types exist per D-06/D-07.
    - Test 2: embedded IDs/codes/paths do not fragment ranking; structured HTTP failures remain `HTTP_502` per D-06/D-07.
    - Test 3: deterministic associated-service contributions are retained as secondary evidence and sum to each type total.
    - Test 4: `UNCLASSIFIED_ERROR` count remains visible even when it is outside the top three per D-08; INFO/WARN rows never enter the taxonomy per D-09.
    - Test 5: equal-count types remain separate and the alphabetical tie-breaker defines the stable top-three boundary.
  </behavior>
  <action>Implement `03_top_normalized_errors.sql` against the Plan 04 registry contract. Query cleaned Parquet using the D-06 primary semantic signature, D-07 secondary service detail, D-08 unclassified visibility, D-09 ERROR-only filter, and D-12 analytical-row boundary. Rank primary types with deterministic count-descending/name-ascending order, retain service contributions without grouping by raw embedded values, and attach the same SQL-derived `unclassified_error_count` total to each top-three row so the fixed three-row result stays intact. Generate the committed table through `--analysis-id top-normalized-errors` after failing tests pass.</action>
  <verify>
    <automated>uv run pytest tests/pipeline/test_analysis.py -q -k 'top_normalized or tie or unclassified' &amp;&amp; uv run python -m pipeline analyze --analysis-id top-normalized-errors --input docs/onboard/datapack/data/app_logs_7days.jsonl --output-root data</automated>
  </verify>
  <acceptance_criteria>
    - Exactly three stable primary error-type rows are returned when the data contains at least three types.
    - Secondary values do not fragment ranking and service contributions reconcile per type.
    - Equal counts have deterministic ordering and the unclassified count remains inspectable.
  </acceptance_criteria>
  <done>A reviewer can rerun the third SQL query and defend the top-three semantic ERROR ranking plus associated services.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reconcile issue occurrences, final actions, and cleaned rows</name>
  <read_first>
    - `docs/onboard/01_Domain_POC.md` — read the repaired/rejected question.
    - `.planning/phases/01-auditable-log-pipeline-analysis/01-CONTEXT.md` — read D-01 through D-05 and D-12.
    - `.planning/phases/01-auditable-log-pipeline-analysis/01-RESEARCH.md` — read pitfall 2, expected source reconciliation, and the second resolved open question.
    - `pipeline/analysis.py`, `tests/pipeline/test_analysis.py` — use the established registry, parameter binding, and deterministic CSV contract.
    - `data/evidence/phase1/quality_ledger.jsonl` and `data/evidence/phase1/schema.json` — treat issues and final actions as distinct counting units.
  </read_first>
  <files>pipeline/sql/04_quality_reconciliation.sql, tests/pipeline/test_analysis.py, data/evidence/phase1/tables/04_quality_reconciliation.csv</files>
  <behavior>
    - Test 1: quality SQL emits record totals by `final_action` separately from issue occurrences by `issue_code` and issue action.
    - Test 2: zero REPAIR remains an explicit record-total row when the canonical run has no mechanically provable repair per D-01.
    - Test 3: physical input lines equal ACCEPT + REPAIR + REJECT, and Parquet rows equal ACCEPT + REPAIR per D-05/D-12.
    - Test 4: issue occurrences may exceed affected records without failing reconciliation, and multiple issues on one rejected row remain visible.
    - Test 5: the quality CSV has stable columns/order, repeats byte-identically, and the complete no-ID analysis command regenerates all four tables.
  </behavior>
  <action>Implement `04_quality_reconciliation.sql` against the Plan 04 registry contract. Read the parameter-bound JSONL ledger, unnest `issues` for issue-occurrence rows, and separately aggregate record totals by final action. Include input, analytical, accepted, repaired, rejected, and both conservation-check rows. Preserve zero-valued action categories so the canonical absence of repairs is explicit evidence rather than an omission, matching the resolved research recommendation and D-01. Generate the fourth table, run the complete no-ID analysis suite, and ensure Python remains orchestration only.</action>
  <verify>
    <automated>uv run pytest tests/pipeline/test_analysis.py -q &amp;&amp; uv run python -m pipeline analyze --input docs/onboard/datapack/data/app_logs_7days.jsonl --output-root data &amp;&amp; uv run ruff check pipeline/analysis.py tests/pipeline/test_analysis.py &amp;&amp; uv run ruff format --check pipeline/analysis.py tests/pipeline/test_analysis.py</automated>
  </verify>
  <acceptance_criteria>
    - Quality output separately labels issue occurrences and record-level actions and includes explicit zero-valued action categories.
    - Both conservation equations reconcile; issue-occurrence totals are never substituted for record totals.
    - All four committed tables regenerate from static SQL, match declared schemas, and repeat stably.
  </acceptance_criteria>
  <done>All four customer questions are reproducible from checked-in SQL and deterministic result tables with honest quality accounting.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Parquet/ledger evidence → DuckDB SQL | Two source formats and two counting units must remain distinct and schema-checked. |
| SQL result → reviewer conclusion | Grouping or tie errors could distort normalized-error or quality claims. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-SC | Tampering | Locked DuckDB/test tooling | high | mitigate | Reuse the Plan 01 human-approved lock and run only checked-in SQL through the existing runner. |
| T-01-10 | Tampering | SQL path/value handling | high | mitigate | Registry selects checked-in SQL; Parquet/ledger paths remain parameter-bound values. |
| T-01-11 | Repudiation | result tables | medium | mitigate | Fixed schemas, deterministic ordering, service/type totals, explicit zero categories, and conservation tests preserve each answer's lineage. |
| T-01-12 | Information Disclosure | aggregate tables | low | accept | Outputs contain aggregate fictional-data dimensions rather than raw messages or request identifiers. |
| T-01-14 | Denial of Service | fixed local queries | low | accept | Four static aggregate queries operate on the bounded 2,923-line dataset and are restartable after visible failure. |
</threat_model>

<verification>
Run the complete analysis tests and CLI; inspect top-three cardinality/tie behavior/service totals, unclassified visibility, separate issue/action counting units, explicit zero REPAIR, both conservation equations, and stable hashes for all four tables.
</verification>

<success_criteria>
- PIPE-09 and PIPE-10 each have dedicated static SQL and a committed result table.
- D-01 through D-09 and D-12 govern error grouping and quality counting without unsupported inference.
- Every ranking and table has explicit deterministic ordering and tie behavior.
- The four-table evidence set is ready for direct manifest/report rendering.
</success_criteria>

<output>
Create `.planning/phases/01-auditable-log-pipeline-analysis/01-05-SUMMARY.md` when done.
</output>
