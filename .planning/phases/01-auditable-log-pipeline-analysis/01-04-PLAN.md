---
phase: 01-auditable-log-pipeline-analysis
plan: 04
type: execute
wave: 4
depends_on:
  - 01-03
files_modified:
  - pipeline/__main__.py
  - pipeline/analysis.py
  - pipeline/sql/01_service_error_counts.sql
  - pipeline/sql/02_daily_error_counts.sql
  - tests/pipeline/test_analysis.py
  - data/evidence/phase1/tables/01_service_error_counts.csv
  - data/evidence/phase1/tables/02_daily_error_counts.csv
autonomous: true
requirements:
  - PIPE-07
  - PIPE-08
  - PIPE-11
estimate:
  tokens: 38000
  raw_tokens: 38000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Static checked-in SQL reads the cleaned Parquet to reproduce the highest-ERROR service and seven UTC daily counts; Python only binds paths, asserts schemas, and writes deterministic tables."
    - "Highest-service ordering is count descending then service ascending, and the first row is the deterministic customer answer."
    - "Daily results cover 2026-07-27 through 2026-08-02, use cleaned ERROR rows, flag only counts strictly greater than twice the seven-day median, record the ratio, and show service contributions without causal claims per D-10 through D-13."
  flagged_assumptions:
    - requirement_id: PIPE-07
      category: unclassified
      status: unresolved
      assumption: "The deterministic probe could not classify the highest-error-service requirement; the plan assumes a deterministic single first-ranked row after count-descending/service-ascending ordering is the reviewer answer."
    - requirement_id: PIPE-08
      category: unclassified
      status: unresolved
      assumption: "The deterministic probe could not classify the daily-pattern requirement; the locked D-11 threshold and D-13 non-causal wording remain the authoritative interpretation."
  prohibitions:
    - statement: "The daily analysis and report MUST NOT present the seven-day median heuristic as a statistical anomaly detector or claim a service caused a flagged day."
      status: unverified
      flagged: true
  artifacts:
    - path: "pipeline/analysis.py"
      provides: "Final static-query registry, parameter-bound DuckDB execution, schema assertions, and deterministic CSV serialization"
      exports: ["AnalysisSpec", "ANALYSIS_SPECS", "run_analysis", "run_all_analyses"]
    - path: "pipeline/sql/01_service_error_counts.sql"
      provides: "Seven-day ERROR counts by service"
    - path: "pipeline/sql/02_daily_error_counts.sql"
      provides: "UTC daily counts, median, ratio, unusual flag, and service contributions"
    - path: "data/evidence/phase1/tables/01_service_error_counts.csv"
      provides: "Recorded result for customer question 1"
    - path: "data/evidence/phase1/tables/02_daily_error_counts.csv"
      provides: "Recorded result for customer question 2"
  key_links:
    - from: "pipeline/analysis.py"
      to: "pipeline/sql/01_service_error_counts.sql, pipeline/sql/02_daily_error_counts.sql"
      via: "fixed AnalysisSpec registry loads static SQL and passes only data paths as parameters"
      pattern: "AnalysisSpec|read_text"
    - from: "pipeline/sql/01_service_error_counts.sql"
      to: "data/processed/logs_clean.parquet"
      via: "parameterized read_parquet over level=ERROR"
      pattern: "read_parquet|ERROR"
    - from: "pipeline/analysis.py"
      to: "data/evidence/phase1/tables"
      via: "stable headers and rows written atomically as UTF-8 CSV"
      pattern: "write_csv_atomic|tables"
---

## Phase Goal

**As a** reviewer, **I want to** run and trace the local log pipeline end-to-end, **so that** every claimed result is reproducible, source-grounded, and defensible.

<objective>
Establish the complete analysis registry and reproduce the highest-ERROR service plus the UTC daily pattern from static SQL.

Purpose: Prove the shared SQL-to-evidence path and the interpretation-sensitive D-11/D-13 daily heuristic before adding the remaining analyses.
Output: A parameter-bound static-SQL runner, the final four-ID registry, two checked-in queries, two committed result tables, and focused tests implementing D-09 through D-13.
</objective>

<execution_context>
@/home/dangnhatminh/.codex/gsd-core/workflows/execute-plan.md
@/home/dangnhatminh/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-auditable-log-pipeline-analysis/01-CONTEXT.md
@.planning/phases/01-auditable-log-pipeline-analysis/01-RESEARCH.md
@.planning/phases/01-auditable-log-pipeline-analysis/01-PATTERNS.md
@.planning/phases/01-auditable-log-pipeline-analysis/01-03-SUMMARY.md
@pipeline/models.py
@pipeline/write_outputs.py
@data/evidence/phase1/schema.json
@docs/onboard/01_Domain_POC.md
</context>

<interfaces>
Consume the clean-record columns in `data/evidence/phase1/schema.json`, especially `source_line`, `event_date_utc`, `service`, `level`, and `error_type`. Define all four final analysis IDs and their SQL/result/parameter/column contracts in Task 1 so Plan 05 adds query implementations without renaming the registry.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reproduce the highest-ERROR service through the final static-SQL registry</name>
  <read_first>
    - `docs/onboard/01_Domain_POC.md` — read the first customer reporting question.
    - `.planning/phases/01-auditable-log-pipeline-analysis/01-CONTEXT.md` — read D-09, D-12, and D-16.
    - `.planning/phases/01-auditable-log-pipeline-analysis/01-RESEARCH.md` — read `Customer analysis`, `Query a generated Parquet file`, and the warning against separate Python calculations.
    - `data/evidence/phase1/schema.json` — bind query columns to the committed Parquet contract.
    - `pipeline/write_outputs.py` — reuse atomic deterministic CSV output.
  </read_first>
  <reversibility rating="costly">CSV headers and query IDs are direct evidence contracts consumed by the report and run manifest.</reversibility>
  <files>pipeline/analysis.py, pipeline/sql/01_service_error_counts.sql, pipeline/__main__.py, tests/pipeline/test_analysis.py, data/evidence/phase1/tables/01_service_error_counts.csv</files>
  <behavior>
    - Test 1: SQL reads only cleaned Parquet, filters `level='ERROR'`, groups by service, and orders `error_count DESC, service ASC`.
    - Test 2: the first output row is the deterministic answer and per-service totals equal the cleaned ERROR row count.
    - Test 3: the CSV has fixed UTF-8 headers and identical bytes on repeated execution.
    - Test 4: caller-supplied paths are bound as values rather than interpolated into SQL text.
    - Test 5: the registry defines all four final IDs, SQL/result paths, parameter sets, and expected columns; `--analysis-id service-error-counts` runs the available first query independently.
  </behavior>
  <action>Create `AnalysisSpec` with `analysis_id`, SQL path, ordered parameter names, result path, and expected columns. Define final registry entries for `service-error-counts`, `daily-error-counts`, `top-normalized-errors`, and `quality-reconciliation` with exact `01` through `04` SQL/result paths and parameter contracts. Implement `run_analysis` using a dedicated DuckDB connection, static SQL, parameter binding, schema assertion, deterministic row order, and atomic CSV replacement; Python must not recalculate aggregates. Add `01_service_error_counts.sql` for cleaned ERROR rows with the stable tie-breaker. Wire `python -m pipeline analyze --analysis-id ID`, while no-ID execution runs every registry entry whose SQL implementation exists and fails clearly for an explicitly requested missing entry. Generate the first table through the selected-ID path after tests pass.</action>
  <verify>
    <automated>uv run pytest tests/pipeline/test_analysis.py -q -k service_error_counts &amp;&amp; uv run python -m pipeline analyze --analysis-id service-error-counts --input docs/onboard/datapack/data/app_logs_7days.jsonl --output-root data</automated>
  </verify>
  <acceptance_criteria>
    - The highest-service answer is generated exclusively by checked-in SQL over cleaned Parquet.
    - Ordering deterministically resolves equal counts and totals equal the Parquet ERROR count.
    - The committed CSV is stable and dataset paths are parameter bound.
    - The complete four-analysis contract is declared for Plan 05 without pretending its unimplemented queries are runnable yet.
  </acceptance_criteria>
  <done>A reviewer can rerun SQL and identify the highest-ERROR service from a committed result table.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reproduce UTC daily counts and the locked unusual-day heuristic</name>
  <read_first>
    - `docs/onboard/01_Domain_POC.md` — read the daily-count/unusual-day question.
    - `.planning/phases/01-auditable-log-pipeline-analysis/01-CONTEXT.md` — read D-10 through D-13 verbatim.
    - `.planning/phases/01-auditable-log-pipeline-analysis/01-RESEARCH.md` — read pitfall 3 and the UTC normalization guidance.
    - `pipeline/analysis.py`, `pipeline/sql/01_service_error_counts.sql`, `tests/pipeline/test_analysis.py` — follow the proven query/result contract.
    - `data/evidence/phase1/schema.json` — use `event_date_utc`, never the source timestamp text date.
  </read_first>
  <files>pipeline/sql/02_daily_error_counts.sql, tests/pipeline/test_analysis.py, data/evidence/phase1/tables/02_daily_error_counts.csv</files>
  <behavior>
    - Test 1: output contains seven UTC dates from 2026-07-27 through 2026-08-02 in ascending order and counts only cleaned ERROR rows per D-10/D-12.
    - Test 2: the median spans all seven daily counts, the observed ratio is recorded, and unusual is true only for strict `daily_error_count > 2 * median_error_count` per D-11.
    - Test 3: each flagged row carries deterministic per-service contributions that sum to the day total, with no causation field per D-13.
    - Test 4: an offset-bearing record whose textual date differs from UTC groups by `event_date_utc`.
  </behavior>
  <action>Add `02_daily_error_counts.sql` against the declared registry contract. Construct the locked seven-date UTC window, left join cleaned ERROR counts so the report has one row per date, calculate the seven-day median, strict threshold, and ratio in SQL, and emit deterministic service contributions for flagged days. Use neutral descriptive fields including `is_unusual_by_2x_median_rule`; D-10 fixes UTC date, D-11 fixes threshold/wording, D-12 excludes rejects, and D-13 limits service detail to contributions. Generate the table through `--analysis-id daily-error-counts` after focused tests pass.</action>
  <verify>
    <automated>uv run pytest tests/pipeline/test_analysis.py -q -k daily &amp;&amp; uv run python -m pipeline analyze --analysis-id daily-error-counts --input docs/onboard/datapack/data/app_logs_7days.jsonl --output-root data &amp;&amp; uv run ruff check pipeline/analysis.py tests/pipeline/test_analysis.py</automated>
  </verify>
  <acceptance_criteria>
    - Exactly seven UTC date rows appear in ascending order and match cleaned ERROR counts.
    - SQL uses strict greater-than against twice the median and records the observed ratio.
    - Flagged-day contributions reconcile to the day total and make no causal claim.
  </acceptance_criteria>
  <done>A reviewer can rerun the UTC daily analysis and inspect the exact descriptive unusual-day rule and service contributions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Generated Parquet → DuckDB SQL | Evidence is trusted only after Plan 03 schema/hash/conservation verification. |
| CLI paths → DuckDB table functions | Caller-controlled paths cross into SQL execution. |
| SQL results → reviewer conclusion | Ordering or interpretation errors could create unsupported customer claims. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-SC | Tampering | Locked DuckDB/test tooling | high | mitigate | Reuse only the Plan 01 human-approved lock and fail locked synchronization on drift. |
| T-01-10 | Tampering | `pipeline/analysis.py` SQL execution | high | mitigate | Load only checked-in static SQL and bind dataset paths as parameters; never concatenate caller input into query text. |
| T-01-11 | Repudiation | customer-answer tables | medium | mitigate | Fixed IDs, SQL/result paths, schemas, deterministic ordering, and aggregate reconciliation preserve production lineage. |
| T-01-12 | Information Disclosure | result tables | low | accept | Tables contain aggregates and service names from fictional data, not full raw messages or identifiers. |
| T-01-13 | Tampering | unusual-day interpretation | medium | mitigate | SQL pins the D-11 strict threshold/ratio and tests service contribution sums; downstream wording remains descriptive per D-13. |
</threat_model>

<verification>
Run focused SQL tests and each selected-ID CLI command; independently check CSV schemas/order, ERROR totals, UTC window, strict median threshold, contribution sums, path parameterization, and stable rerun bytes.
</verification>

<success_criteria>
- PIPE-07 and PIPE-08 each have dedicated static SQL and a committed deterministic result table.
- D-10 through D-13 control the daily calculation and interpretation exactly.
- The full four-analysis registry contract is ready for Plan 05 without changing IDs, paths, or schemas.
- No result is produced by a separate Python calculation path.
</success_criteria>

<output>
Create `.planning/phases/01-auditable-log-pipeline-analysis/01-04-SUMMARY.md` when done.
</output>
