---
phase: 01-auditable-log-pipeline-analysis
plan: 01
subsystem: data-pipeline
tags: [python, uv, duckdb, parquet, pytest, ruff]
requires: []
provides:
  - "Locked local Python toolchain with DuckDB, pytest, and Ruff"
  - "Immutable one-line tracer from JSONL provenance to Parquet, SQL, and manifest evidence"
affects: ["Phase 1 plans 02-06", pipeline, evidence]
actuals:
  tokens: 8677
  tasks: 2
  commits: 3
tech-stack:
  added: ["DuckDB 1.5.5", "pytest 9.1.1", "Ruff 0.16.2", uv]
  patterns: ["atomic generated artifacts", "source pre/post SHA-256 integrity checks", "static parameter-bound DuckDB SQL"]
key-files:
  created:
    - pyproject.toml
    - uv.lock
    - pipeline/__main__.py
    - pipeline/sql/00_tracer_service_error_counts.sql
    - tests/pipeline/test_tracer.py
  modified:
    - .gitignore
key-decisions:
  - "Use a deterministic trace manifest with output-root-relative artifact paths so fresh output roots produce identical evidence bytes."
  - "Keep the tracer as a small argparse module command with an explicit write-boundary check for docs/onboard."
patterns-established:
  - "Generate JSONL, Parquet, CSV, and manifests via complete-file replacement under an explicit output root."
  - "Execute checked-in SQL through a dedicated DuckDB connection with bound runtime values."
requirements-completed: [RPRO-01, RPRO-02, PIPE-01, PIPE-05, PIPE-06, PIPE-11]
coverage:
  - id: D1
    description: "Approved locked Python toolchain resolves and synchronizes deterministically."
    requirement: RPRO-01
    verification:
      - kind: integration
        ref: "uv lock --check && uv sync --locked"
        status: pass
    human_judgment: false
  - id: D2
    description: "One physical immutable source line is traced with source-line and SHA-256 evidence."
    requirement: RPRO-02
    verification:
      - kind: e2e
        ref: "tests/pipeline/test_tracer.py#test_trace_preserves_source_provenance_and_normalizes_real_error"
        status: pass
    human_judgment: false
  - id: D3
    description: "The tracer writes linked ledger, typed Parquet, static-SQL CSV, and manifest artifacts."
    requirement: PIPE-11
    verification:
      - kind: e2e
        ref: "tests/pipeline/test_tracer.py#test_trace_writes_content_linked_ledger_parquet_sql_and_manifest"
        status: pass
    human_judgment: false
  - id: D4
    description: "Fresh tracer output roots are byte-stable and writes under supplied inputs are rejected."
    requirement: PIPE-05
    verification:
      - kind: e2e
        ref: "tests/pipeline/test_tracer.py#test_trace_is_stable_across_fresh_output_roots"
        status: pass
      - kind: e2e
        ref: "tests/pipeline/test_tracer.py#test_trace_rejects_output_inside_immutable_source_tree"
        status: pass
    human_judgment: false
duration: 4min
completed: 2026-08-11
status: complete
---

# Phase 01 Plan 01: Approved Toolchain and Immutable Log Tracer Summary

**A locked Python/DuckDB tracer carries a real immutable error line through normalized typed Parquet, static SQL, and content-linked evidence.**

## Performance

- **Duration:** 4 min
- **Started:** 2026-08-11T03:18:00Z
- **Completed:** 2026-08-11T03:22:02Z
- **Tasks:** 2/2
- **Files modified:** 7

## Accomplishments

- Recorded the approved `uv`, DuckDB 1.5.5, pytest 9.1.1, and Ruff 0.16.2 stack in `pyproject.toml` and generated `uv.lock` with uv 0.12.3.
- Implemented `python -m pipeline trace` with source-line selection, a 1 MiB line limit, pre/post SHA-256 checks, UTC/error normalization, and atomic output writes.
- Proved a real source line reaches a JSONL ledger, fixed-schema DuckDB Parquet, parameter-bound static SQL result, and hash-linked manifest without modifying supplied inputs.

## Task Commits

1. **Task 1: Confirm the four package identities before bootstrap or install** - Approval checkpoint satisfied explicitly; no repository change or commit was required.
2. **Task 2 (RED): Prove one immutable log line through the complete local evidence path** - `b3faa89` (`test`)
3. **Task 2 (GREEN): Prove one immutable log line through the complete local evidence path** - `19cde70` (`feat`)

## Files Created/Modified

- `pyproject.toml` and `uv.lock` - Exact approved dependency constraints and resolved lockfile.
- `pipeline/__main__.py` - Trace CLI, validation/normalization, atomic writers, Parquet generation, SQL execution, and manifest creation.
- `pipeline/sql/00_tracer_service_error_counts.sql` - Deterministically ordered error-count query over the tracer Parquet file.
- `tests/pipeline/test_tracer.py` - End-to-end source integrity, artifact linkage, determinism, and safety tests.
- `.gitignore` - Excludes the local virtual environment and Python bytecode generated by the verified test run.

## Decisions Made

- Keep manifest artifact paths relative to the chosen output root, while retaining the canonical source path, so identical inputs produce byte-identical artifacts across fresh roots.
- Store `event_date_utc` as a typed Parquet `DATE`; Python readers therefore receive `datetime.date` rather than a string.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Established the package/test import boundary**
- **Found during:** Task 2
- **Issue:** Pytest's collection path shadowed the new `pipeline` package and prevented the intended module import.
- **Fix:** Added the package marker and explicit project-root pytest path configuration.
- **Files modified:** `pipeline/__init__.py`, `pyproject.toml`
- **Verification:** All tracer tests import and pass.
- **Committed in:** `19cde70`

**2. [Rule 1 - Bug] Corrected the static DuckDB aggregate/window query**
- **Found during:** Task 2
- **Issue:** DuckDB rejected a window expression that referenced the aggregate alias in the same SELECT scope.
- **Fix:** Introduced a `service_counts` CTE before applying row numbering and ordering.
- **Files modified:** `pipeline/sql/00_tracer_service_error_counts.sql`
- **Verification:** The SQL result test passes with one ranked service row.
- **Committed in:** `19cde70`

**3. [Rule 1 - Test correctness] Asserted the typed Parquet date correctly**
- **Found during:** Task 2
- **Issue:** The initial test expected a string for a DuckDB `DATE` column, but the typed reader correctly returned `datetime.date`.
- **Fix:** Asserted the typed date value instead of weakening the Parquet schema.
- **Files modified:** `tests/pipeline/test_tracer.py`
- **Verification:** The raw/normalized-field test passes.
- **Committed in:** `19cde70`

**4. [Rule 3 - Generated output hygiene] Ignored local Python runtime artifacts**
- **Found during:** Task 2
- **Issue:** The approved uv/pytest run created local virtual-environment and bytecode artifacts.
- **Fix:** Added narrow ignore rules for `.venv/`, `__pycache__/`, and Python bytecode.
- **Files modified:** `.gitignore`
- **Verification:** No generated test artifacts remain untracked.
- **Committed in:** `19cde70`

**Total deviations:** 4 auto-fixed (2 Rule 1, 2 Rule 3).
**Impact on plan:** All fixes were required for a runnable, typed, and clean tracer; no Phase 2+ scope was added.

## Verification

- `uv lock --check` — passed.
- `uv sync --locked` — passed (7 packages checked).
- `uv run pytest tests/pipeline/test_tracer.py -q` — passed (5 tests).
- `uv run ruff check pipeline/__main__.py tests/pipeline/test_tracer.py` — passed.
- `uv run ruff format --check pipeline/__main__.py tests/pipeline/test_tracer.py` — passed.
- The sorted `docs/onboard` SHA-256 inventory remained `cc9ee5ab372e2aa70cc987bd3f2a0be012dcd653e673c3e7840b26072bb2c6fd` before and after execution; a direct module invocation also confirmed every manifest artifact exists and matches its recorded hash.

## Known Stubs

None.

## Issues Encountered

None remaining.

## User Setup Required

None - no external service configuration is required.

## Next Phase Readiness

The tracer establishes the output, provenance, and evidence contracts that Plan 01-02 can expand from a single physical source line to the full dataset.

## Self-Check: PASSED

- All five planned source/config/test artifacts exist and both TDD commits are present in Git history.
- The committed lock, test, lint, format, source-integrity, and manifest-link verification gates all passed.

---
*Phase: 01-auditable-log-pipeline-analysis*
*Completed: 2026-08-11*
