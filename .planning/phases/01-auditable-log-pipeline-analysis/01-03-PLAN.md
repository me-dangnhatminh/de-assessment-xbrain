---
phase: 01-auditable-log-pipeline-analysis
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - pipeline/__main__.py
  - pipeline/integrity.py
  - pipeline/normalize.py
  - pipeline/write_outputs.py
  - tests/pipeline/test_pipeline_outputs.py
  - data/processed/logs_clean.parquet
  - data/evidence/phase1/source_manifest.json
  - data/evidence/phase1/quality_ledger.jsonl
  - data/evidence/phase1/schema.json
autonomous: true
requirements:
  - RPRO-02
  - PIPE-04
  - PIPE-05
  - PIPE-06
estimate:
  tokens: 43000
  raw_tokens: 43000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Valid `Z` and offset timestamps preserve their raw representations, derive the same aware UTC instant/date, and record normalization separately from repair per D-04/D-10."
    - "Only ERROR rows receive stable primary taxonomy fields; known signatures retain secondary values, unmatched valid errors remain `UNCLASSIFIED_ERROR`, and INFO/WARN taxonomy fields are null per D-06 through D-09."
    - "Only ACCEPT/REPAIR records reach fixed-schema Parquet; every physical line remains in the ledger and conservation is `input = ACCEPT + REPAIR + REJECT = parquet + REJECT` per D-05/D-12."
    - "Repeated canonical runs preserve every supplied hash and produce stably ordered, byte-identical ledger, schema, source-manifest, and Parquet artifacts on the locked platform."
  flagged_assumptions:
    - requirement_id: PIPE-05
      category: empty
      status: unresolved
      assumption: "Row conservation is explicit for the supplied non-empty input; the artifacts do not define whether an empty source should emit a zero-row Parquet file or fail closed."
    - requirement_id: PIPE-06
      category: unclassified
      status: unresolved
      assumption: "The deterministic probe could not classify the structured-dataset requirement; the fixed schema and Parquet rationale must remain reviewer-visible and queryable."
  prohibitions:
    - statement: "The pipeline MUST NOT alter supplied bytes, invent missing analytical values, or relabel normalization as repair to make quality results appear cleaner."
      status: unverified
      flagged: true
  artifacts:
    - path: "pipeline/integrity.py"
      provides: "Sorted supplied-input SHA-256 inventory and output-root/source guards"
      exports: ["sha256_file", "inventory_supplied_inputs", "assert_source_unchanged", "validate_output_root"]
    - path: "pipeline/normalize.py"
      provides: "Aware UTC and ERROR-only semantic normalization"
      exports: ["normalize_timestamp", "normalize_error"]
    - path: "pipeline/write_outputs.py"
      provides: "Atomic deterministic ledger, schema, JSON, CSV, and DuckDB Parquet writers"
      exports: ["write_json_atomic", "write_jsonl_atomic", "write_csv_atomic", "write_parquet_atomic", "write_schema"]
    - path: "data/evidence/phase1/quality_ledger.jsonl"
      provides: "One final-action record per physical source line with every issue and normalization"
    - path: "data/processed/logs_clean.parquet"
      provides: "Typed cleaned analytical records in source-line order"
    - path: "data/evidence/phase1/schema.json"
      provides: "Queryable fixed schema, provenance descriptions, and Parquet rationale"
  key_links:
    - from: "pipeline/validation.py"
      to: "pipeline/normalize.py"
      via: "only ACCEPT/REPAIR records proceed after all issues determine final_action"
      pattern: "Disposition\.(ACCEPT|REPAIR)"
    - from: "pipeline/normalize.py"
      to: "pipeline/write_outputs.py"
      via: "CleanRecord retains raw fields plus derived UTC/error dimensions"
      pattern: "CleanRecord|write_parquet_atomic"
    - from: "pipeline/integrity.py"
      to: "data/evidence/phase1/source_manifest.json"
      via: "sorted byte-level inventory before and after generated writes"
      pattern: "sha256|source_manifest"
    - from: "pipeline/__main__.py"
      to: "data/processed/logs_clean.parquet"
      via: "cmd_run validates, normalizes, asserts conservation, and atomically publishes generated artifacts"
      pattern: "cmd_run|write_parquet_atomic"
---

## Phase Goal

**As a** reviewer, **I want to** run and trace the local log pipeline end-to-end, **so that** every claimed result is reproducible, source-grounded, and defensible.

<objective>
Materialize the complete validation decisions as source-integrity evidence, a deterministic quality ledger, and a typed cleaned Parquet dataset.

Purpose: Produce the auditable analytical boundary that every later SQL answer consumes, without source mutation or manual cleanup.
Output: Integrity, normalization, and deterministic writer modules; full-run orchestration; committed source manifest, ledger, schema, and Parquet evidence implementing D-04 and D-06 through D-10/D-12.
</objective>

<execution_context>
@/home/dangnhatminh/.codex/gsd-core/workflows/execute-plan.md
@/home/dangnhatminh/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-auditable-log-pipeline-analysis/01-CONTEXT.md
@.planning/phases/01-auditable-log-pipeline-analysis/01-RESEARCH.md
@.planning/phases/01-auditable-log-pipeline-analysis/01-PATTERNS.md
@.planning/phases/01-auditable-log-pipeline-analysis/01-02-SUMMARY.md
@pipeline/models.py
@pipeline/ingest.py
@pipeline/validation.py
@pipeline/__main__.py
@docs/onboard/datapack/README.md
</context>

<interfaces>
Consume `Disposition`, `Issue`, `Normalization`, `SourceEnvelope`, `LedgerEntry`, and `CleanRecord` exactly as established in Plan 02. Clean-record fields are `source_line`, `source_sha256`, `timestamp_raw`, `timestamp_utc`, `event_date_utc`, `timestamp_offset_raw`, `service`, `level`, `message_raw`, `request_id`, `trace_id`, `error_type`, `error_code`, `related_component`, `path`, and `error_parameters_json`.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Normalize accepted analytical rows and prove deterministic writers</name>
  <read_first>
    - `.planning/phases/01-auditable-log-pipeline-analysis/01-CONTEXT.md` — read D-04 and D-06 through D-10 plus D-12.
    - `.planning/phases/01-auditable-log-pipeline-analysis/01-RESEARCH.md` — read `Stable Artifact Construction`, pitfalls 3–5, and aware UTC guidance.
    - `.planning/phases/01-auditable-log-pipeline-analysis/01-PATTERNS.md` — use the assigned normalize/writer boundaries and fixed DuckDB connection pattern.
    - `pipeline/models.py` and `pipeline/validation.py` — consume the exact clean-record and final-action contracts.
  </read_first>
  <reversibility rating="costly">The cleaned Parquet schema is a generated contract used by every SQL/report consumer, but remains regenerable from immutable inputs.</reversibility>
  <files>pipeline/normalize.py, pipeline/write_outputs.py, tests/pipeline/test_pipeline_outputs.py</files>
  <behavior>
    - Test 1: valid `Z` and `+07:00` timestamps preserve raw text/offset, normalize to the same aware UTC instant, and derive `event_date_utc` per D-04/D-10.
    - Test 2: recognized ERROR tokens `SMTPConnRefused`, `PaymentDeclined`, `ConnTimeout`, `NullPointer`, and `HTTP 502` map to stable primary types including `HTTP_502`; codes/components/paths stay secondary per D-06/D-07/D-09.
    - Test 3: a valid unmatched ERROR remains analytical with `UNCLASSIFIED_ERROR` and raw message, while INFO/WARN taxonomy fields are null per D-08/D-09.
    - Test 4: JSON, JSONL, schema, CSV, and fixed-schema Parquet writers replace atomically and yield stable bytes for the same ordered inputs on the locked platform.
  </behavior>
  <action>Implement `normalize_timestamp` with aware UTC conversion and separate normalization evidence per D-04/D-10. Implement deterministic ERROR-only parsers for the five signatures, structured `HTTP_502`, secondary `error_code`, `related_component`, `path`, sorted `error_parameters_json`, and the D-08 fallback. Implement atomic same-directory temporary writes followed by replacement for JSON, JSONL, CSV, and DuckDB Parquet. Build Parquet from the declared clean-record schema ordered by `source_line` with a dedicated DuckDB connection and explicit compression/writer settings; implement `write_schema` with names, DuckDB types, nullability, provenance descriptions, formats, and the concise Parquet rationale. Begin with failing tests.</action>
  <verify>
    <automated>uv run pytest tests/pipeline/test_pipeline_outputs.py -q -k 'normaliz or writer or parquet or schema' &amp;&amp; uv run ruff check pipeline/normalize.py pipeline/write_outputs.py tests/pipeline/test_pipeline_outputs.py</automated>
  </verify>
  <acceptance_criteria>
    - Raw timestamp/message values coexist with derived UTC/error fields exactly as D-04 and D-06 through D-10 require.
    - Unmatched valid ERROR rows remain analytical and visible as `UNCLASSIFIED_ERROR`; non-ERROR rows have null taxonomy fields.
    - Writers are atomic, deterministically ordered, and emit the fixed Parquet/schema contracts.
  </acceptance_criteria>
  <done>A reviewer-facing analytical row can be normalized and written to typed Parquet without changing its raw provenance.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire the full run with source integrity and row conservation</name>
  <read_first>
    - `.planning/phases/01-auditable-log-pipeline-analysis/01-CONTEXT.md` — read D-05, D-10, and D-12.
    - `.planning/phases/01-auditable-log-pipeline-analysis/01-RESEARCH.md` — read the expected reconciliation, stable-artifact pattern, and Security Domain controls.
    - `pipeline/models.py`, `pipeline/ingest.py`, `pipeline/validation.py` — use the Plan 02 validation stream and dispositions unchanged.
    - `pipeline/normalize.py`, `pipeline/write_outputs.py`, `pipeline/__main__.py` — wire the Task 1 output contracts into the existing CLI.
    - `docs/onboard/datapack/README.md` — preserve the entire supplied tree.
  </read_first>
  <files>pipeline/integrity.py, pipeline/__main__.py, tests/pipeline/test_pipeline_outputs.py</files>
  <behavior>
    - Test 1: a sorted inventory includes every regular file below `docs/onboard`, and all hashes match before and after the run.
    - Test 2: outputs inside `docs/onboard` are rejected before writing, while an explicit generated root receives only known paths through atomic replacement.
    - Test 3: only ACCEPT/REPAIR records reach Parquet; ledger rows equal physical lines and `input = accepted + repaired + rejected = parquet + rejected` per D-05/D-12.
    - Test 4: two full runs in fresh roots produce identical source manifest, ledger, schema, and Parquet bytes on the locked platform.
    - Test 5: `python -m pipeline integrity` and `python -m pipeline run` are independently runnable and print final-action plus unclassified-error totals.
  </behavior>
  <action>Implement `sha256_file`, `inventory_supplied_inputs`, `assert_source_unchanged`, and `validate_output_root`; inventory every regular file below `docs/onboard` in sorted relative-path order and reject outputs anywhere inside that tree. Wire `cmd_integrity` and `cmd_run` into the existing CLI. `run` must hash supplied inputs before/after, consume every Plan 02 validation result, normalize only ACCEPT/REPAIR rows, write ledger/schema/source manifest/Parquet to a validated output root, assert both conservation equations before publication completes, and print final-action and unclassified-error totals. D-12 keeps rejects out of analytical rows and never assigns them an invented date. Start from failing integration tests.</action>
  <verify>
    <automated>uv run pytest tests/pipeline/test_validation.py tests/pipeline/test_pipeline_outputs.py tests/pipeline/test_tracer.py -q &amp;&amp; uv run ruff check pipeline tests/pipeline &amp;&amp; uv run ruff format --check pipeline tests/pipeline</automated>
  </verify>
  <acceptance_criteria>
    - The complete supplied-file inventory and pre/post hashes remain unchanged.
    - One ledger row exists per physical line; only ACCEPT/REPAIR rows reach Parquet; both record-level equations reconcile.
    - UTC dates cover 2026-07-27 through 2026-08-02 and the unclassified ERROR count remains visible.
    - Independent integrity/run commands and deterministic fresh-root reruns pass.
  </acceptance_criteria>
  <done>A reviewer can run the full immutable-source transformation into reconciled ledger and Parquet artifacts.</done>
</task>

<task type="auto">
  <name>Task 3: Publish the canonical base evidence snapshot</name>
  <read_first>
    - `.planning/phases/01-auditable-log-pipeline-analysis/01-CONTEXT.md` — read D-16 and D-17 for committed evidence expectations.
    - `pipeline/__main__.py` — use only the tested `run` stage; do not introduce a separate generation path.
    - `docs/onboard/datapack/data/app_logs_7days.jsonl` and `docs/onboard/datapack/README.md` — canonical read-only input and immutability contract.
  </read_first>
  <files>data/processed/logs_clean.parquet, data/evidence/phase1/source_manifest.json, data/evidence/phase1/quality_ledger.jsonl, data/evidence/phase1/schema.json</files>
  <action>After Tasks 1–2 pass, run the canonical source through `python -m pipeline run` with output root `data`. Check that the committed artifacts are exactly the outputs of the tested production path, not hand-edited fixtures. Run the same command into a fresh temporary root and compare stable artifact hashes and row counts before accepting the repository snapshot.</action>
  <verify>
    <automated>uv run python -m pipeline run --input docs/onboard/datapack/data/app_logs_7days.jsonl --output-root data &amp;&amp; uv run pytest tests/pipeline/test_pipeline_outputs.py -q -k 'canonical or determin or conservation' &amp;&amp; git diff --exit-code -- docs/onboard</automated>
  </verify>
  <acceptance_criteria>
    - All four declared base-evidence paths exist outside the supplied tree and originate from the tested run command.
    - Ledger/action totals, Parquet rows, date coverage, and schema agree with the focused tests and conservation equations.
    - A fresh-root rerun matches the committed stable artifact hashes on the locked platform.
    - `git diff -- docs/onboard` is empty.
  </acceptance_criteria>
  <done>The deterministic ledger, schema, source manifest, and cleaned Parquet snapshot are committed and regenerable from unchanged inputs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Validated record → analytical Parquet | Rejected data must not cross the quality boundary; raw provenance remains attributable. |
| CLI paths → generated writers | Input/output aliasing could mutate sources or unrelated files. |
| Generated artifacts → later SQL | Stable schema, order, and integrity must be established before analysis. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-SC | Tampering | Locked DuckDB/test tooling | high | mitigate | Consume only the human-approved Plan 01 lock; no new installation occurs. |
| T-01-05 | Tampering | output path validation and writers | high | mitigate | Resolve paths, reject outputs within `docs/onboard`, hash supplied files before/after, and atomically replace only explicit generated paths. |
| T-01-07 | Repudiation | ledger and Parquet publication | medium | mitigate | Retain raw provenance, all issues/normalizations, action totals, source hashes, schema, and both conservation equations. |
| T-01-08 | Information Disclosure | generated ledger | low | accept | Full fictional-source ledger content is required reviewer evidence; ordinary logs omit unrelated bulk data. |
| T-01-14 | Denial of Service | bounded local run | low | accept | The supplied dataset is 2,923 lines and each line is already capped before parsing; the local batch is bounded and restartable. |
</threat_model>

<verification>
Run focused normalization/writer tests, the full pipeline tests, and the canonical CLI; query the Parquet schema with DuckDB, compare source hashes before/after, assert conservation, and rerun in a fresh root to prove stable content.
</verification>

<success_criteria>
- D-04 and D-06 through D-10 define the UTC/error normalization exactly.
- Accepted/repaired rows alone reach Parquet; rejects remain disclosed and reconciled per D-12.
- Source inventory, ledger, schema, and cleaned Parquet are deterministic committed evidence outside the immutable supplied tree.
- The base evidence is ready for static SQL without schema or interface changes.
</success_criteria>

<output>
Create `.planning/phases/01-auditable-log-pipeline-analysis/01-03-SUMMARY.md` when done.
</output>
