---
phase: 01-auditable-log-pipeline-analysis
plan: 08
type: execute
wave: 8
depends_on:
  - 01-07
files_modified:
  - pipeline/integrity.py
  - pipeline/__main__.py
  - pipeline/manifest.py
  - tests/pipeline/test_evidence.py
  - tests/pipeline/test_end_to_end.py
autonomous: true
gap_closure: true
requirements:
  - RPRO-02
  - PIPE-05
estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Production `run` and `all` accept only the canonical supplied JSONL path; an identical byte-for-byte JSONL copied elsewhere in the repository is rejected before cleanup or evidence publication."
    - "Standalone verification authenticates the persisted input descriptor by allowed supplied-inventory membership and by a fresh SHA-256 comparison with the live canonical file, the source manifest, and the supplied inventory, closing RPRO-02 and D-17."
    - "The run-manifest builder and verifier each query the generated Parquet artifact for `COUNT(*)`; no run-manifest Parquet count is copied from another persisted declaration."
    - "Verification independently derives ACCEPT, REPAIR, and REJECT record totals from the ledger and proves input/action and analytical/Parquet conservation per D-05/D-12."
    - "Rebuilding derived manifest metadata cannot authenticate a foreign input descriptor or a falsified source-manifest Parquet count, and repeated build/verify calls retain stable ordering and results."
  flagged_assumptions:
    - requirement_id: RPRO-02
      category: concurrency
      status: unresolved
      assumption: "The documented workflow has one writer per output root and does not define an atomic multi-file snapshot for a verifier racing a concurrent writer; this plan fails closed on observed mismatches but does not claim writer/verifier coordination."
    - requirement_id: PIPE-05
      category: empty
      status: unresolved
      assumption: "The canonical supplied log is non-empty; the source artifacts do not define a successful wholly empty pipeline evidence set. Verification must reject malformed or incomplete ledger/Parquet evidence without claiming general empty-dataset semantics."
    - requirement_id: PIPE-05
      category: adjacency
      status: unresolved
      assumption: "Ledger source-line ordering remains governed by completed Plans 01-02/01-03; action-count verification is order-independent and does not add a second ordering contract."
  artifacts:
    - path: "pipeline/integrity.py"
      provides: "Canonical supplied-log constant and reusable production-input path guard"
      exports: ["CANONICAL_LOG_INPUT", "require_canonical_log_input"]
    - path: "pipeline/__main__.py"
      provides: "Fail-closed run/all entry-point binding before cleanup or generated writes"
      contains: "require_canonical_log_input"
    - path: "pipeline/manifest.py"
      provides: "Live input-descriptor authentication, measured Parquet cardinality, and independently derived ledger-action conservation"
      contains: "_verify_input_binding"
    - path: "tests/pipeline/test_evidence.py"
      provides: "Adversarial manifest-rebuild regressions for foreign input descriptors and falsified Parquet counts"
    - path: "tests/pipeline/test_end_to_end.py"
      provides: "Regression proving a foreign repository-local JSONL cannot pass production run/all"
  key_links:
    - from: "pipeline/__main__.py"
      to: "pipeline/integrity.py"
      via: "`cmd_run()` and `cmd_all()` resolve the requested input through the canonical supplied-log guard before cleanup or publication"
      pattern: "require_canonical_log_input"
    - from: "pipeline/manifest.py"
      to: "data/evidence/phase1/source_manifest.json and docs/onboard/datapack/data/app_logs_7days.jsonl"
      via: "persisted input path membership plus live SHA-256 comparison against the corresponding supplied-inventory entry"
      pattern: "_verify_input_binding|sha256_file"
    - from: "pipeline/manifest.py"
      to: "data/processed/logs_clean.parquet"
      via: "a dedicated DuckDB connection executes parameter-bound `COUNT(*)` during manifest build and verification"
      pattern: "_parquet_row_count|read_parquet"
    - from: "pipeline/manifest.py"
      to: "data/evidence/phase1/quality_ledger.jsonl"
      via: "verification parses every ledger row and independently counts final actions before checking both conservation equations"
      pattern: "_ledger_action_counts|final_action"
---

## Phase Goal

**As a** reviewer, **I want to** run the complete log pipeline and customer analysis, **so that** I can defend every result from immutable source evidence.

<objective>
Close the remaining two Phase 1 evidence-integrity blockers by authenticating the exact processed input and independently measuring the analytical and ledger rows represented by the manifests.

Purpose: A passing verifier must prove that the evidence originated from the supplied immutable log bytes and that its recorded conservation totals describe the actual generated artifacts rather than mutually consistent persisted claims.
Output: Canonical production-input guards, live input-descriptor verification, direct Parquet and ledger measurements, and adversarial regressions implementing RPRO-02, PIPE-05, D-05, D-12, D-14, and D-17.
</objective>

<execution_context>
@/home/dangnhatminh/.codex/gsd-core/workflows/execute-plan.md
@/home/dangnhatminh/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/01-auditable-log-pipeline-analysis/01-CONTEXT.md
@.planning/phases/01-auditable-log-pipeline-analysis/01-RESEARCH.md
@.planning/phases/01-auditable-log-pipeline-analysis/01-VERIFICATION.md
@.planning/phases/01-auditable-log-pipeline-analysis/01-REVIEW.md
@.planning/phases/01-auditable-log-pipeline-analysis/01-07-SUMMARY.md
@pipeline/integrity.py
@pipeline/__main__.py
@pipeline/manifest.py
@tests/pipeline/test_evidence.py
@tests/pipeline/test_end_to_end.py
</context>

<interfaces>
`pipeline.integrity.inventory_supplied_inputs(supplied_root: Path = SUPPLIED_ROOT) -> list[dict[str, str]]` returns stable `path`/`sha256` entries relative to `docs/onboard`. `pipeline.__main__.cmd_run(arguments)` currently publishes `source_manifest.json` with `input.path`, `input.sha256`, `source_inventory`, and `row_counts`; `cmd_all(arguments)` performs cleanup and then calls every production stage. `pipeline.manifest.build_run_manifest(output_root: Path) -> dict[str, Any]` may rebuild derived metadata, while `verify_run_manifest(output_root: Path) -> None` is the independent fail-closed boundary and raises `ManifestVerificationError`. Preserve all existing public CLI flags, manifest field names, and public function signatures.
</interfaces>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Bind production evidence to the canonical supplied log end to end</name>
  <read_first>
    - `.planning/phases/01-auditable-log-pipeline-analysis/01-VERIFICATION.md` — read the first failed truth, foreign-input spot-check, RPRO-02 result, key-link failure, and gap `missing` list.
    - `.planning/phases/01-auditable-log-pipeline-analysis/01-CONTEXT.md` — read D-14 and D-17; preserve the canonical/stage command and regenerated-evidence contracts.
    - `pipeline/integrity.py` — extend the existing repository/supplied-root path and hash authority; do not create another root or SHA-256 implementation.
    - `pipeline/__main__.py` — inspect `DEFAULT_INPUT`, `cmd_run()`, `cmd_all()`, and parser wiring so rejection occurs before `--clean` or generated writes.
    - `pipeline/manifest.py` — inspect `_verify_source_inventory()`, `_read_json()`, `_manifest_payload()`, and `verify_run_manifest()`; keep persisted JSON untrusted.
    - `tests/pipeline/test_evidence.py` and `tests/pipeline/test_end_to_end.py` — extend the existing public-CLI and rebuild-after-forgery test patterns.
  </read_first>
  <reversibility rating="costly">The production CLI input contract and persisted evidence trust boundary are evaluator-facing contracts shared by Make, README, tests, and downstream submission verification.</reversibility>
  <files>pipeline/integrity.py, pipeline/__main__.py, pipeline/manifest.py, tests/pipeline/test_evidence.py, tests/pipeline/test_end_to_end.py</files>
  <behavior>
    - Test 1: canonical `run` and `all` still succeed and persist the canonical repository-relative input path plus its live SHA-256.
    - Test 2 (`test_run_and_all_reject_foreign_repo_local_input`): copy the canonical JSONL bytes to a temporary file inside the repository but outside `docs/onboard`, invoke both production entry points, require exit code 2 before cleanup/publication, and remove the temporary file in a `finally` block.
    - Test 3 (`test_manifest_verification_rejects_foreign_input_descriptor_after_rebuild`): replace the persisted input path/hash with a foreign repository-local descriptor, rebuild all derived run-manifest metadata, and require `ManifestVerificationError` for allowed-membership failure.
    - Test 4 (`test_manifest_verification_rejects_forged_input_hash_after_rebuild`): retain the canonical path but falsify its persisted input SHA-256, rebuild, and require verification to reject the byte-identity mismatch.
    - Test 5 (`test_manifest_build_and_verification_are_repeatable`): matching canonical evidence can be built and verified twice with identical manifest content, preserving existing stable inventory ordering and read-only idempotence without mutating Parquet or ledger artifacts.
  </behavior>
  <action>Write the named failing regressions first. In `pipeline/integrity.py`, define `CANONICAL_LOG_INPUT` from `SUPPLIED_ROOT` and add `require_canonical_log_input(input_path: Path) -> Path`, resolving aliases and accepting only the exact canonical supplied file. Reuse this guard from `cmd_run()` and at the beginning of `cmd_all()` before `--clean`, source inventory, validation, or generated writes; keep `trace` and `validate` fixture-friendly because the blocker concerns production `run`/`all`. In `pipeline/manifest.py`, add `_verify_input_binding(source_manifest, live_inventory)` and invoke it from `verify_run_manifest()` after loading both persisted manifests. Require `input` to be an object with string `path` and 64-hex `sha256`, require the normalized path to equal the canonical repository-relative log path and map to the corresponding `docs/onboard` inventory member without traversal, compute the live file SHA-256, and require equality among the descriptor digest, live digest, and matching live/source-manifest inventory digest. Keep the existing three-way complete inventory comparison from Plan 01-07. Raise stable `ManifestVerificationError` messages naming membership versus hash mismatch without dumping source contents. This closes RPRO-02 and strengthens D-17 while retaining the D-14 command surface.</action>
  <verify>
    <automated>if command -v uv &gt;/dev/null 2&gt;&amp;1; then uv run --locked pytest tests/pipeline/test_end_to_end.py::test_run_and_all_reject_foreign_repo_local_input tests/pipeline/test_evidence.py::test_manifest_verification_rejects_foreign_input_descriptor_after_rebuild tests/pipeline/test_evidence.py::test_manifest_verification_rejects_forged_input_hash_after_rebuild tests/pipeline/test_evidence.py::test_manifest_build_and_verification_are_repeatable -q; else .venv/bin/python -m pytest tests/pipeline/test_end_to_end.py::test_run_and_all_reject_foreign_repo_local_input tests/pipeline/test_evidence.py::test_manifest_verification_rejects_foreign_input_descriptor_after_rebuild tests/pipeline/test_evidence.py::test_manifest_verification_rejects_forged_input_hash_after_rebuild tests/pipeline/test_evidence.py::test_manifest_build_and_verification_are_repeatable -q; fi</automated>
  </verify>
  <acceptance_criteria>
    - The same-byte foreign repository-local JSONL is rejected by both `run` and `all` before cleanup or evidence publication, while canonical invocations remain successful.
    - Verification rejects an out-of-inventory persisted input path and rejects a false input digest even after `build_run_manifest()` rebuilds hashes and `run_id` around each forgery.
    - Successful verification proves exact canonical membership and byte identity against the persisted descriptor and supplied inventory, in addition to Plan 01-07's whole-tree comparison.
    - Both repeated canonical builds are equal and two consecutive verification calls succeed without writing evidence.
    - The tests leave `docs/onboard` and the temporary repository-local foreign file unchanged/absent after completion.
  </acceptance_criteria>
  <done>A passing production evidence chain can originate only from the canonical supplied JSONL path and current live bytes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove row conservation from live Parquet and ledger contents</name>
  <read_first>
    - `.planning/phases/01-auditable-log-pipeline-analysis/01-VERIFICATION.md` — read the second failed truth, forged-Parquet-count spot-check, PIPE-05 result, and disconnected row-conservation data-flow entry.
    - `.planning/phases/01-auditable-log-pipeline-analysis/01-CONTEXT.md` — read D-05, D-12, and D-17 for final-action precedence, analytical-row boundary, and independent consistency checks.
    - `pipeline/manifest.py` — inspect `_manifest_payload()`, `_artifact()`, `_verify_row_counts()`, and `verify_run_manifest()`; remove reliance on cross-comparing persisted Parquet declarations.
    - `pipeline/models.py` — use the established ACCEPT, REPAIR, and REJECT values rather than duplicating a new disposition vocabulary.
    - `tests/pipeline/test_evidence.py` — extend the current count-tampering cases with the exact source-manifest-falsification/rebuild sequence.
    - `data/evidence/phase1/source_manifest.json` and `data/evidence/phase1/quality_ledger.jsonl` — preserve field names and one-final-action-per-line shape; do not hand-edit committed evidence.
  </read_first>
  <files>pipeline/manifest.py, tests/pipeline/test_evidence.py</files>
  <behavior>
    - Test 1: `_manifest_payload()` obtains the Parquet artifact `row_count` and run-manifest `row_counts.parquet` from `SELECT COUNT(*) FROM read_parquet(?)`, not from `source_manifest.json`.
    - Test 2 (`test_manifest_verification_rejects_falsified_source_parquet_count_after_rebuild`): increment only `source_manifest.row_counts.parquet`, rebuild the run manifest, and require `verify_run_manifest()` to fail because live Parquet cardinality remains unchanged.
    - Test 3 (`test_manifest_verification_derives_ledger_action_conservation`): verification counts ACCEPT, REPAIR, and REJECT directly from every JSONL ledger row and requires input = ACCEPT + REPAIR + REJECT and Parquet = ACCEPT + REPAIR independently of both manifests.
    - Test 4: malformed ledger JSON, a non-object ledger row, a missing/unknown `final_action`, or a truncated/missing Parquet file raises `ManifestVerificationError` rather than producing a default count.
    - Test 5 (`test_manifest_build_and_verification_are_repeatable`, shared with Task 1): canonical zero-REPAIR evidence verifies successfully; repeated build/verify calls remain byte-stable and do not mutate Parquet or ledger artifacts.
  </behavior>
  <action>Write the falsified-count and independently derived conservation regressions before implementation. Add `_parquet_row_count(path: Path) -> int` using a dedicated DuckDB connection and parameter-bound `SELECT COUNT(*) FROM read_parquet(?)`; wrap missing, unreadable, or invalid Parquet failures as `ManifestVerificationError`. During `_manifest_payload()`, measure the live Parquet count and use it for both the Parquet artifact descriptor and the run-manifest `row_counts.parquet`, leaving the source-manifest declaration as an independent claim that verification must compare. Add `_ledger_action_counts(path: Path) -> dict[str, int]` that streams each ledger line once, parses strict JSON objects, accepts only the established `Disposition` values, and raises on blank/malformed/non-object/missing/unknown action evidence. Rewrite `_verify_row_counts()` so verification performs a fresh Parquet count, derives ledger ACCEPT/REPAIR/REJECT totals, and requires each derived total to equal both persisted layers; require ledger lines = input = ACCEPT + REPAIR + REJECT and live Parquet = ACCEPT + REPAIR per D-05/D-12. Ensure the actual Parquet count also equals the Parquet artifact descriptor. Keep issue-occurrence aggregation in SQL 04 unchanged because final-action records, not nested issues, govern conservation. Do not add a fallback count on read failure. This makes the D-17 verifier independent of mutable row-count declarations.</action>
  <verify>
    <automated>if command -v uv &gt;/dev/null 2&gt;&amp;1; then uv run --locked pytest tests/pipeline/test_evidence.py::test_manifest_verification_rejects_falsified_source_parquet_count_after_rebuild tests/pipeline/test_evidence.py::test_manifest_verification_derives_ledger_action_conservation tests/pipeline/test_evidence.py::test_manifest_build_and_verification_are_repeatable tests/pipeline/test_evidence.py::test_manifest_verification_accepts_matching_live_source_inventory -q; else .venv/bin/python -m pytest tests/pipeline/test_evidence.py::test_manifest_verification_rejects_falsified_source_parquet_count_after_rebuild tests/pipeline/test_evidence.py::test_manifest_verification_derives_ledger_action_conservation tests/pipeline/test_evidence.py::test_manifest_build_and_verification_are_repeatable tests/pipeline/test_evidence.py::test_manifest_verification_accepts_matching_live_source_inventory -q; fi</automated>
  </verify>
  <acceptance_criteria>
    - Build-time and verify-time Parquet counts each come from reading the generated artifact through DuckDB.
    - Falsifying only `source_manifest.row_counts.parquet`, rebuilding the run manifest, and invoking verification produces a named failure.
    - Verification independently derives ledger final-action totals, validates both conservation equations, and cross-checks the source manifest, run manifest, and Parquet artifact descriptor.
    - Canonical explicit `REPAIR=0` remains valid; malformed or degenerate evidence fails closed without an error-suppressing default.
    - The focused regressions and every pre-existing manifest tamper test pass without changing SQL 04 or any supplied input.
  </acceptance_criteria>
  <done>A passing verifier proves actual Parquet cardinality and ledger-derived record conservation rather than agreement among persisted declarations.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| CLI `--input` path → production run/all | A caller-controlled repository path must not select evidence bytes outside the canonical supplied log. |
| Persisted input descriptor/inventories → verification verdict | Generated JSON claims are untrusted until path membership and bytes are authenticated from the live supplied tree. |
| Persisted row counts → live ledger/Parquet | Mutable manifest declarations must not authenticate their own conservation claims. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-SC | Tampering | Existing uv/DuckDB/pytest/Ruff execution | high | mitigate | Add no package install; retain the committed human-approved lock and run the established locked-uv/existing-venv verification path. |
| T-01-24 | Tampering | `cmd_run()` / `cmd_all()` input selection | high | mitigate | Resolve the input before cleanup or writes and require exact equality with the canonical supplied-log path; test a same-byte foreign repository-local file. |
| T-01-25 | Spoofing | `source_manifest.input` | high | mitigate | Require canonical inventory membership and recompute the live SHA-256 before accepting the descriptor, source inventory, or rebuilt run manifest. |
| T-01-26 | Tampering | Parquet/ledger row-count evidence | high | mitigate | Measure Parquet with DuckDB and final actions from strict ledger parsing during verification; test a source-count forgery followed by manifest rebuild. |
| T-01-27 | Tampering | Concurrent writer/verifier on one output root | low | accept | The documented local workflow is single-writer and verification follows atomic artifact publication; there is no supported concurrent-write contract, and observable mixed-state reads fail closed. |
| T-01-28 | Information Disclosure | Integrity error messages | low | accept | Relative evidence paths and count categories are required reviewer evidence; diagnostics identify the failed layer without emitting source records. |

## OWASP ASVS L1 Control Mapping and Blocking Gate

Under the default ASVS L1 policy, every high-severity threat in this register (`T-01-SC`, `T-01-24`, `T-01-25`, and `T-01-26`) is an execution blocker. The executor must not mark this plan complete or publish a passing verification result until the listed mitigation is present at the cited trust boundary and its blocking gate passes; failed adversarial tests keep the corresponding threat open.

| ASVS L1 control objective | Threat | Required implementation evidence | Blocking adversarial test or gate |
|---|---|---|---|
| Validate security-relevant input at the primary trust boundary | T-01-24 canonical input binding | `require_canonical_log_input()` is invoked by `cmd_run()` and at the start of `cmd_all()` before cleanup or evidence writes. | `test_run_and_all_reject_foreign_repo_local_input` |
| Authenticate persisted security metadata against live trusted data | T-01-25 persisted-manifest authentication | `_verify_input_binding()` requires the canonical inventory member and equality among descriptor, live-file, and supplied-inventory SHA-256 values. | `test_manifest_verification_rejects_foreign_input_descriptor_after_rebuild` and `test_manifest_verification_rejects_forged_input_hash_after_rebuild` |
| Verify integrity claims from the live protected artifacts, independently of persisted declarations | T-01-26 live Parquet/ledger count verification | `_parquet_row_count()` and `_ledger_action_counts()` feed `_verify_row_counts()` so both conservation equations use freshly measured artifact contents. | `test_manifest_verification_rejects_falsified_source_parquet_count_after_rebuild` and `test_manifest_verification_derives_ledger_action_conservation` |
| Preserve the approved dependency and execution baseline | T-01-SC locked toolchain integrity | No package install is added; focused tests run through locked `uv` when available or the established existing environment, followed by the full `make verify-phase1` gate. | Both task-level automated gates and `make verify-phase1` pass. |
</threat_model>

## Artifacts this phase produces

- New symbols: `pipeline.integrity.CANONICAL_LOG_INPUT`, `pipeline.integrity.require_canonical_log_input(input_path)`, `pipeline.manifest._verify_input_binding(source_manifest, live_inventory)`, `pipeline.manifest._parquet_row_count(path)`, and `pipeline.manifest._ledger_action_counts(path)`.
- Updated symbols: `cmd_run()` and `cmd_all()` reject non-canonical production input before cleanup/publication; `_manifest_payload()` records measured Parquet cardinality; `_verify_row_counts()` and `verify_run_manifest()` authenticate input identity and derive conservation from live artifacts.
- New test functions in `tests/pipeline/test_end_to_end.py`: `test_run_and_all_reject_foreign_repo_local_input`.
- New test functions in `tests/pipeline/test_evidence.py`: `test_manifest_verification_rejects_foreign_input_descriptor_after_rebuild`, `test_manifest_verification_rejects_forged_input_hash_after_rebuild`, `test_manifest_verification_rejects_falsified_source_parquet_count_after_rebuild`, `test_manifest_verification_derives_ledger_action_conservation`, and `test_manifest_build_and_verification_are_repeatable`.
- Persisted fields retained and newly authenticated: source-manifest `input.path`, `input.sha256`, `source_inventory`, `row_counts.input`, `row_counts.accept`, `row_counts.repair`, `row_counts.reject`, `row_counts.parquet`; run-manifest `source_inventory`, `row_counts`, and Parquet artifact `row_count`.
- CLI surface: no new command or flag. Existing `run` and `all --input` now accept only `docs/onboard/datapack/data/app_logs_7days.jsonl`; `verify --input` remains syntactically compatible while verification treats the persisted descriptor and live canonical bytes as authority.
- No new dependency, SQL file, generated output path, manifest field, external API surface, UI surface, or ORM/schema-push artifact is introduced.

## Multi-Source Coverage Audit

| Source | ID | Feature / requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Reviewer-runnable and defensible immutable-source log pipeline/customer analysis | 01-01–01-08 | COVERED | Plan 08 closes the two remaining evidence-integrity blockers; executed feature scope remains in Plans 01–07. |
| REQ | RPRO-01 | Locked clean-checkout environment and commands | 01-01, 01-06 | COVERED | Clean-uv execution remains explicitly behavior-unverified on this host; no fabricated observation is added. |
| REQ | RPRO-02 | Supplied inputs remain unchanged and hashes authenticate processed bytes | 01-01, 01-03, 01-06–01-08 | COVERED | Task 1 binds production input and verifies its persisted descriptor against live membership/bytes. |
| REQ | PIPE-01 | Provenance-preserving physical-line ingestion | 01-01, 01-02 | COVERED | Completed scope unchanged. |
| REQ | PIPE-02 | Stable issue detection | 01-02 | COVERED | Completed scope unchanged. |
| REQ | PIPE-03 | Explicit accept/repair/reject rules | 01-02 | COVERED | Completed scope unchanged. |
| REQ | PIPE-04 | Per-record quality ledger | 01-02, 01-03 | COVERED | Completed ledger shape is consumed directly by Task 2. |
| REQ | PIPE-05 | Row conservation and deterministic reruns | 01-01, 01-03, 01-06, 01-08 | COVERED | Task 2 independently measures Parquet and ledger contents; repeated verification/build behavior is exercised. |
| REQ | PIPE-06 | Typed Parquet and schema rationale | 01-01, 01-03, 01-06 | COVERED | Existing artifact unchanged; Task 2 reads it through DuckDB. |
| REQ | PIPE-07 | Highest-ERROR service analysis | 01-04, 01-06 | COVERED | Completed scope unchanged. |
| REQ | PIPE-08 | Daily pattern and qualified unusual-day rule | 01-04, 01-06 | COVERED | Completed scope unchanged. |
| REQ | PIPE-09 | Top normalized error analysis | 01-05, 01-06 | COVERED | Completed scope unchanged. |
| REQ | PIPE-10 | Rejected/repaired quality reconciliation | 01-05, 01-06 | COVERED | SQL evidence remains unchanged; Task 2 independently checks final-action conservation at the verifier boundary. |
| REQ | PIPE-11 | Dataset/SQL/result/manifest evidence chain | 01-01, 01-04–01-06 | COVERED | Existing direct answer links remain unchanged. |
| RESEARCH | — | Complete sorted supplied inventory and immutable generated-output boundary | 01-03, 01-06–01-08 | COVERED | Task 1 authenticates the selected input member within the already-live inventory. |
| RESEARCH | — | Deterministic manifest construction and content-derived run ID | 01-06–01-08 | COVERED | Tasks explicitly retain stable ordering and repeatable build/verify behavior. |
| RESEARCH | — | Ledger/Parquet analytical boundary and two conservation equations | 01-03, 01-05, 01-08 | COVERED | Task 2 measures both artifact sides independently per D-05/D-12. |
| RESEARCH | — | Provenance validation, UTC/error normalization, checked-in SQL, evidence-only report | 01-02–01-06 | COVERED | Outside the two verification gaps and preserved. |
| CONTEXT | D-01–D-05 | Conservative disposition, duplicates, normalization evidence, and action precedence | 01-02, 01-03, 01-05, 01-08 | COVERED | Task 2 consumes the established D-05 final-action values without changing policy. |
| CONTEXT | D-06–D-09 | ERROR normalization and unclassified visibility | 01-01, 01-03, 01-05, 01-06 | COVERED | Completed scope unchanged. |
| CONTEXT | D-10–D-13 | UTC days, analytical-row boundary, and qualified unusual-day analysis | 01-03, 01-04, 01-06, 01-08 | COVERED | Task 2 enforces D-12's ACCEPT/REPAIR analytical boundary at verification. |
| CONTEXT | D-14–D-16 | Canonical/stage commands, report, and direct evidence chain | 01-01, 01-06, 01-08 | COVERED | Task 1 hardens run/all without renaming the D-14 surface. |
| CONTEXT | D-17 | Regenerated snapshot verifies hashes/internal consistency | 01-03, 01-06–01-08 | COVERED | Both tasks prevent rebuilt metadata from authenticating false input or row-count claims. |

## Verification scope notes

- Repeated verification and stable manifest ordering are exercised explicitly in Task 1; they are not silently inferred from a single successful invocation.
- Empty/degenerate canonical evidence and concurrent writer/verifier coordination remain unresolved assumptions in `must_haves.flagged_assumptions`; this plan fails closed on malformed or visibly mixed evidence and makes no broader contract claim.
- The clean-uv observations in `01-VERIFICATION.md` remain `PRESENT_BEHAVIOR_UNVERIFIED` until an actual clean uv-backed run is observed.
- Review warnings WR-01 and WR-02 are supporting findings rather than authoritative verification blockers and are not included in this closure plan.

<verification>
Run both task-focused adversarial test groups, then run `make verify-phase1`. Confirm the canonical snapshot regenerates, the full pytest and Ruff gates pass, the verifier succeeds twice on unmodified evidence, the two forged-manifest rebuild sequences fail, the foreign repository-local input is removed, and `git diff --exit-code -- docs/onboard` remains clean.
</verification>

<success_criteria>
- RPRO-02 is closed by production path binding plus live membership/hash authentication of the persisted input descriptor.
- PIPE-05 is closed by build-time and verify-time Parquet measurement plus verifier-derived ledger action totals and conservation equations.
- Both exact adversarial sequences from `01-VERIFICATION.md` fail after rebuilding derived manifest metadata.
- Existing canonical commands, evidence fields, deterministic ordering, analyses, report links, and supplied bytes remain intact.
- All unresolved edge assumptions are visible without being represented as completed behavior.
</success_criteria>

<output>
Create `.planning/phases/01-auditable-log-pipeline-analysis/01-08-SUMMARY.md` when done.
</output>
