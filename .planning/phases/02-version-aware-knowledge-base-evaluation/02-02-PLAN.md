---
phase: 02-version-aware-knowledge-base-evaluation
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - kb/eval_cases.py
  - kb/eval_runner.py
  - kb/eval_report.py
  - tests/kb/test_eval_cases.py
  - tests/kb/test_eval_runner.py
  - sop/kb_update_sop.md
  - data/evidence/phase2/eval_results.json
  - data/evidence/phase2/eval_report.md
  - Makefile
autonomous: false
requirements:
  - KB-07
  - KB-08
  - KB-09
  - KB-10
  - KB-11
  - KB-12
  - SOP-01
  - SOP-02
estimate:
  tokens: 38000
  raw_tokens: 38000
  tasks: 3
  confidence: medium
must_haves:
  truths:
    - "10 predeclared evaluation questions cover 4 direct lookup, 3 multi-source synthesis, 2 version trap (POL-01), and 1 out-of-scope refusal — with expected sources and pass/partial/fail criteria for each."
    - "All 10 evaluations are executed programmatically (retrieval-only, no LLM) with ranked BM25 results, retrieval-hit scoring, and groundedness diagnosis as two independent dimensions."
    - "The POL-01 version trap questions demonstrate that current-policy search returns v2 content and explicitly identifies v1 as superseded when historical mode is used."
    - "The out-of-scope question receives a clear 'not found in the supplied documents' outcome rather than an invented answer."
    - "Evaluation results are saved in both structured JSON and a rendered Markdown report with inline queries, expected answers, actual retrieved chunks, scores, and diagnoses."
    - "A one-page English SOP covers update cadence, owner, operator, approver, new/revised docs, validation, re-indexing, regression evaluation, approval, rollback, and history retention."
  flagged_assumptions:
    - requirement_id: KB-09
      category: unclassified
      status: resolved
      assumption: "The requirement asks for 'at least three' recorded executions but decision D-11 runs all 10 — this exceeds the minimum and is acceptable."
  prohibitions:
    - "Do not use LLM/Bedrock calls in evaluation — Phase 2 is retrieval-only."
    - "Do not modify source documents under docs/onboard/datapack/."
    - "Do not invent operational answers for the out-of-scope question."
  artifacts:
    - path: "kb/eval_cases.py"
      provides: "10 predeclared evaluation cases with questions, types, expected sources, and criteria"
      contains: "EVAL_CASES"
    - path: "kb/eval_runner.py"
      provides: "Programmatic evaluation executor that runs all cases against the FTS5 index"
      contains: "run_evaluation"
    - path: "kb/eval_report.py"
      provides: "Generates both JSON results and Markdown report from evaluation output"
      contains: "render_eval_report"
    - path: "data/evidence/phase2/eval_results.json"
      provides: "Structured evaluation results with per-question scoring"
    - path: "data/evidence/phase2/eval_report.md"
      provides: "Human-readable evaluation report with inline evidence"
    - path: "sop/kb_update_sop.md"
      provides: "One-page English SOP for KB updates per SOP-01 and SOP-02"
  key_links:
    - from: "kb/eval_runner.py"
      to: "kb/search.py"
      via: "Calls search_current() and search_all() for each evaluation case"
      pattern: "search_current|search_all"
    - from: "kb/eval_runner.py"
      to: "kb/eval_cases.py"
      via: "Iterates over EVAL_CASES to execute each query"
      pattern: "EVAL_CASES"
    - from: "kb/eval_report.py"
      to: "data/evidence/phase2/eval_results.json"
      via: "Serializes structured evaluation output"
      pattern: "eval_results.json"
---

## Phase Goal

**As a** reviewer, **I want to** inspect predeclared evaluation cases with scored retrieval traces and a concise update SOP, **so that** I can assess KB quality and understand maintenance responsibilities.

<objective>
Author 10 evaluation cases across 4 required types, execute them programmatically against the FTS5 index, score retrieval hit and groundedness independently, and produce evidence in both JSON and Markdown. Write a one-page English SOP for KB updates.

Purpose: Fulfill KB-07 through KB-12 (evaluation coverage, execution, scoring, version-trap proof, out-of-scope handling) and SOP-01/SOP-02 (update procedure with roles).
Output: Evaluation cases, full execution results, a rendered report, and the KB update SOP.
</objective>

<execution_context>
@/home/dangnhatminh/.codex/gsd-core/workflows/execute-plan.md
@/home/dangnhatminh/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/02-version-aware-knowledge-base-evaluation/02-CONTEXT.md
@.planning/phases/02-version-aware-knowledge-base-evaluation/02-DISCUSSION-LOG.md
@.planning/phases/02-version-aware-knowledge-base-evaluation/02-01-PLAN.md
@docs/onboard/datapack/reading/02_rag_eval_basics.md
@docs/onboard/datapack/data/docs/POL-01_chinh_sach_backup_v1.md
@docs/onboard/datapack/data/docs/POL-01_chinh_sach_backup_v2.md
</context>

<tasks>

<task type="implement" tdd="true">
  <name>Task 1: Author 10 predeclared evaluation cases with expected sources and criteria</name>
  <read_first>
    - `.planning/phases/02-version-aware-knowledge-base-evaluation/02-CONTEXT.md` — read decisions D-10 through D-13 for evaluation design.
    - `docs/onboard/datapack/reading/02_rag_eval_basics.md` — understand the 4 evaluation question types, retrieval hit vs groundedness, and scoring criteria.
    - `.planning/REQUIREMENTS.md` — read KB-07, KB-08 for predeclared cases structure.
    - All 8 source documents — to author questions whose answers are grounded in the actual content.
  </read_first>
  <reversibility rating="reversible">Evaluation cases are declarative data; they can be updated without rebuilding the index or changing the search implementation.</reversibility>
  <files>kb/eval_cases.py, tests/kb/test_eval_cases.py</files>
  <behavior>
    - Test 1: EVAL_CASES contains exactly 10 cases.
    - Test 2: Distribution matches 4-3-2-1: 4 direct_lookup, 3 multi_source, 2 version_trap, 1 out_of_scope.
    - Test 3: Each case has: question (str), type (enum), expected_sources (list of doc_id + section), expected_answer_keywords (list of key facts), pass_criteria (str), partial_criteria (str), fail_criteria (str).
    - Test 4: The 2 version_trap cases reference POL-01 and test current-vs-superseded behavior.
    - Test 5: The 1 out_of_scope case asks about something not covered in any supplied document.
    - Test 6: The 3 multi_source cases require information from 2+ different doc_id sources.
    - Test 7: All expected_sources reference actual doc_ids and sections that exist in the chunked index.
  </behavior>
  <action>Create `kb/eval_cases.py` with an `EvalCase` dataclass and a `EVAL_CASES` list of 10 instances. Design questions grounded in the actual document content:

  **Direct lookup (4):**
  1. "Thời gian sao lưu dữ liệu hiện tại là mấy giờ?" — Expected: POL-01 v2, Quy định (23:30)
  2. "Ngưỡng CRITICAL của tỉ lệ ERROR là bao nhiêu?" — Expected: GUIDE-01, Ngưỡng cảnh báo (>5%)
  3. "Khi nào phải escalation lên mức 3?" — Expected: SOP-02, Luồng escalation (P1 or P2 >4h)
  4. "Job batch-report chạy lúc mấy giờ?" — Expected: RUN-01, Lịch chạy (23:00)

  **Multi-source synthesis (3):**
  5. "Khi payment-api gặp lỗi 502, quy trình xử lý gồm những bước nào?" — Expected: FAQ-01 §3 + SOP-01 §Quy trình + SOP-02 §Phân mức
  6. "Các yêu cầu bảo mật khi truy cập database production?" — Expected: POL-02 §Quy định + GUIDE-01 §Dashboard
  7. "Quy trình xử lý khi job báo cáo cuối ngày lỗi NullPointer?" — Expected: RUN-01 §Khi job lỗi + FAQ-01 §4

  **Version trap (2):**
  8. "Thời gian lưu giữ bản sao lưu theo chính sách hiện hành?" — Expected: POL-01 v2 only (30 ngày); v1 answer (7 ngày) would be wrong
  9. "So sánh chính sách sao lưu phiên bản cũ và mới?" — Expected: Both POL-01 v1 and v2 needed (historical comparison)

  **Out of scope (1):**
  10. "Chi phí hàng tháng cho dịch vụ cloud backup là bao nhiêu?" — Expected: Not found in supplied documents

  Write tests validating structure, distribution, and reference correctness.</action>
  <verify>
    <automated>uv run pytest tests/kb/test_eval_cases.py -q &amp;&amp; uv run ruff check kb/eval_cases.py tests/kb/test_eval_cases.py &amp;&amp; uv run ruff format --check kb/eval_cases.py tests/kb/test_eval_cases.py</automated>
  </verify>
  <acceptance_criteria>
    - 10 cases with the exact 4-3-2-1 type distribution.
    - Each case has grounded expected sources and answer keywords from actual document content.
    - Version trap cases specifically target the POL-01 v1/v2 supersession.
    - Out-of-scope case asks about information genuinely absent from all 8 documents.
    - All expected_sources are verifiable against the actual chunk inventory.
  </acceptance_criteria>
  <done>10 predeclared evaluation cases are authored with grounded expectations, correct type distribution, and testable criteria.</done>
</task>

<task type="implement" tdd="true">
  <name>Task 2: Execute all 10 evaluations programmatically with dual-dimension scoring</name>
  <read_first>
    - `.planning/phases/02-version-aware-knowledge-base-evaluation/02-CONTEXT.md` — read decisions D-11, D-12, D-13 for execution and scoring design.
    - `docs/onboard/datapack/reading/02_rag_eval_basics.md` — retrieval hit vs groundedness definitions.
    - `kb/search.py` — understand search_current() and search_all() interfaces (from Plan 01).
    - `kb/eval_cases.py` — the 10 cases authored in Task 1.
  </read_first>
  <reversibility rating="reversible">Evaluation execution is stateless against the built index. Re-running produces the same results given the same index.</reversibility>
  <files>kb/eval_runner.py, kb/eval_report.py, tests/kb/test_eval_runner.py, data/evidence/phase2/eval_results.json, data/evidence/phase2/eval_report.md, Makefile</files>
  <behavior>
    - Test 1: `run_evaluation()` executes all 10 cases and returns 10 EvalResult objects.
    - Test 2: Each EvalResult has: case_id, question, type, query_used, retrieved_chunks (with bm25 scores), retrieval_hit_score (pass/partial/fail), groundedness_score (pass/partial/fail), diagnosis (str), expected_sources, actual_sources_found.
    - Test 3: For version trap case 8 (current retention period), retrieval_hit is pass if POL-01 v2 chunk appears in top-k with current-mode search.
    - Test 4: For version trap case 9 (comparison), retrieval_hit is pass if both POL-01 v1 and v2 appear when using all-mode search.
    - Test 5: For out-of-scope case 10, retrieval_hit is fail (no relevant chunks) and the diagnosis explicitly states "not found in the supplied documents".
    - Test 6: `render_eval_report()` produces a Markdown file with inline query, expected answer, actual retrieved chunks, scores, and diagnosis for each case.
    - Test 7: The JSON output contains all 10 results with complete metadata (timestamps, index path, chunk counts).
    - Test 8: Re-running evaluation against the same index produces identical JSON output (deterministic).
  </behavior>
  <action>Implement `kb/eval_runner.py` with `run_evaluation(db_path, cases, top_k=5)` that for each case: (1) determines search mode (current for direct_lookup and version_trap type 8; all for version_trap type 9 that needs comparison; current for multi_source; none for out_of_scope), (2) calls the appropriate search function, (3) scores retrieval_hit by checking if any expected_source doc_id+section appears in top-k results, (4) scores groundedness by checking if expected_answer_keywords appear in retrieved chunk content, (5) generates diagnosis text. For the out-of-scope case, search returns empty or irrelevant results → retrieval_hit=fail, groundedness=fail, diagnosis="Not found in the supplied documents." Implement `kb/eval_report.py` with `render_eval_report(results, output_dir)` producing both `eval_results.json` (structured) and `eval_report.md` (human-readable with section per question showing query, type, expected, actual chunks with scores, retrieval hit, groundedness, diagnosis). Add `eval` subcommand to `kb/__main__.py` and `kb-eval` Makefile target. Wire `phase2` target to chain `kb-build` → `kb-eval`.</action>
  <verify>
    <automated>uv run pytest tests/kb/test_eval_runner.py -q &amp;&amp; uv run ruff check kb/ tests/kb/ &amp;&amp; uv run ruff format --check kb/ tests/kb/ &amp;&amp; uv run python -m kb build --docs-dir docs/onboard/datapack/data/docs --output-dir data/evidence/phase2 &amp;&amp; uv run python -m kb eval --db data/evidence/phase2/index.sqlite --output-dir data/evidence/phase2</automated>
  </verify>
  <acceptance_criteria>
    - All 10 evaluations execute programmatically without LLM calls.
    - Retrieval hit and groundedness are scored as two independent dimensions per case.
    - Version trap case 8 proves POL-01 v2 wins current-policy; case 9 proves both versions are accessible.
    - Out-of-scope case 10 produces an explicit "not found" diagnosis.
    - JSON and Markdown evidence files are written to data/evidence/phase2/.
    - Evaluation is deterministic (same index → same results).
  </acceptance_criteria>
  <done>All 10 evaluation cases are executed with dual-dimension scoring, version-trap proof, and out-of-scope handling — evidenced in both JSON and Markdown formats.</done>
</task>

<task type="implement" tdd="false">
  <name>Task 3: Write the one-page English KB Update SOP</name>
  <read_first>
    - `.planning/REQUIREMENTS.md` — read SOP-01 and SOP-02 requirements exactly.
    - `.planning/phases/02-version-aware-knowledge-base-evaluation/02-CONTEXT.md` — understand the KB architecture for update procedure context.
    - `kb/__main__.py` — understand the build and eval commands that the SOP will reference.
  </read_first>
  <reversibility rating="reversible">The SOP is a standalone document; changes do not affect code or evaluation results.</reversibility>
  <files>sop/kb_update_sop.md</files>
  <behavior>N/A — this is a documentation task, not a code task.</behavior>
  <action>Write `sop/kb_update_sop.md` as a one-page-or-shorter English document covering:
  1. **Purpose and scope** — What this SOP governs (adding/updating operational docs in the KB).
  2. **Roles** — Owner (who is accountable), Technical Operator (who executes), Approver (who signs off). Must name specific roles from the Sao Do organization (e.g., IT Operations Manager, On-duty Operator, Operations Head).
  3. **Update cadence** — Quarterly review cycle for all documents; ad-hoc for urgent policy changes.
  4. **Procedure for new documents** — File naming, metadata header format, placement, rebuild, eval regression.
  5. **Procedure for revised documents** — Version increment, supersession metadata, effective date, rebuild, eval regression.
  6. **Metadata and version validation** — Required fields checklist, supersession phrase for replacements, effective_date format.
  7. **Re-indexing** — Command to rebuild (`python -m kb build`), verify chunk count, FTS5 integrity.
  8. **Regression evaluation** — Run `python -m kb eval`, compare scores to baseline, investigate degradations.
  9. **Approval gate** — Approver reviews eval results before production use.
  10. **Rollback and history** — How to revert (restore previous file versions, rebuild), superseded docs remain in index as is_current=0.

  Keep to one page (roughly 400–500 words). Use clear headings and numbered steps.</action>
  <verify>
    <automated>wc -w sop/kb_update_sop.md | awk '{if ($1 > 600) exit 1}'</automated>
    <human-check>SOP covers all SOP-01 and SOP-02 requirements, names specific roles with cadence, and fits one page.</human-check>
  </verify>
  <acceptance_criteria>
    - One page or shorter in English.
    - Covers: new docs, revised docs, metadata validation, re-indexing, regression evaluation, approval, rollback, history retention.
    - Names update cadence (quarterly + ad-hoc).
    - Identifies owner, operator, and approver roles without unstated assumptions.
    - References actual project commands (`python -m kb build`, `python -m kb eval`).
  </acceptance_criteria>
  <done>The KB Update SOP is written, meeting SOP-01 and SOP-02 requirements within the one-page constraint.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Evaluation case expected_sources → chunk index | Expected sources must reference actual doc_ids and sections present in the built index. |
| Evaluation scoring → report output | Scores must be deterministic and honestly reflect retrieval quality — no score inflation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-05 | Spoofing | Evaluation scoring | medium | mitigate | Scoring logic is transparent, testable, and deterministic. Both retrieval hit and groundedness are independently verifiable from the saved ranked traces. |
| T-02-06 | Repudiation | Evaluation evidence | medium | mitigate | Full ranked retrieval traces are saved in eval_results.json with BM25 scores, chunk content, and source attribution. Any claim can be verified against the saved trace. |
| T-02-07 | Information Disclosure | SOP role names | low | accept | The SOP names fictional organizational roles (Sao Do Finance IT Operations); no real personnel data is exposed. |
</threat_model>

## Artifacts this plan produces

- Symbols: `EvalCase`, `EvalResult`, `EVAL_CASES`; `run_evaluation`, `score_retrieval_hit`, `score_groundedness`; `render_eval_report`, `render_eval_json`.
- CLI surface: `python -m kb eval --db <path> --output-dir <path>`.
- EvalCase fields: `case_id`, `question`, `type` (direct_lookup|multi_source|version_trap|out_of_scope), `expected_sources`, `expected_answer_keywords`, `search_mode`, `pass_criteria`, `partial_criteria`, `fail_criteria`.
- EvalResult fields: `case_id`, `question`, `type`, `query_used`, `retrieved_chunks` (list with chunk_id, doc_id, section, bm25_score, content_snippet), `retrieval_hit_score`, `groundedness_score`, `diagnosis`, `expected_sources`, `actual_sources_found`.
- Output files: `data/evidence/phase2/eval_results.json`, `data/evidence/phase2/eval_report.md`, `sop/kb_update_sop.md`.
- Makefile targets: `kb-eval`, `phase2`.

<verification>
Run the full eval pipeline end-to-end: build index → run evaluation → verify JSON and Markdown outputs exist with 10 results. Verify version trap cases show correct POL-01 behavior. Verify SOP word count fits one page.
</verification>

<success_criteria>
- 10 evaluation cases with correct 4-3-2-1 type distribution are authored and executed.
- Retrieval hit and groundedness are independently scored per case.
- POL-01 version trap is demonstrably proven in evaluation results.
- Out-of-scope case receives explicit "not found" rather than an invented answer.
- Evidence is saved in both structured JSON and human-readable Markdown.
- The SOP covers all required topics within one page with named roles and cadence.
</success_criteria>

<output>
Create `.planning/phases/02-version-aware-knowledge-base-evaluation/02-02-SUMMARY.md` when done.
</output>
